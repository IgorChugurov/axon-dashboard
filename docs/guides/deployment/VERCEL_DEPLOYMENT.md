# 🚀 Деплой на Vercel

Инструкция по подключению репозитория к Vercel и настройке поддомена `admin.axondigital.xyz`.

---

## 📋 Шаг 1: Подключение репозитория к Vercel

### 1.1. Войдите в Vercel

1. Откройте [vercel.com](https://vercel.com)
2. Войдите в свой аккаунт (или создайте новый)
3. Перейдите в Dashboard

### 1.2. Импортируйте проект

1. Нажмите **"Add New..."** → **"Project"**
2. Выберите **"Import Git Repository"**
3. Найдите ваш репозиторий `OUR-pack/axon-dashboard` (или подключите GitHub аккаунт, если еще не подключен)
4. Нажмите **"Import"**

### 1.3. Настройка проекта

Vercel автоматически определит Next.js проект. Проверьте настройки:

- **Framework Preset**: `Next.js`
- **Root Directory**: `./` (корень проекта)
- **Build Command**: `pnpm build` (или `npm run build`)
- **Output Directory**: `.next` (автоматически)
- **Install Command**: `pnpm install` (или `npm install`)

---

## 🔐 Шаг 2: Настройка переменных окружения

### 2.1. Добавьте переменные в Vercel

В настройках проекта перейдите в **Settings** → **Environment Variables** и добавьте:

#### Обязательные переменные:

```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
```

#### Опциональные переменные:

```env
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
NEXT_PUBLIC_SITE_URL=https://admin.axondigital.xyz
```

### 2.2. Настройка окружений

Для каждой переменной выберите окружения:

- ✅ **Production**
- ✅ **Preview** (опционально)
- ✅ **Development** (опционально)

⚠️ **Важно**: `SUPABASE_SERVICE_ROLE_KEY` должен быть доступен только в **Production** окружении!

---

## 🌐 Шаг 3: Настройка поддомена admin.axondigital.xyz

### 3.1. Добавьте домен в Vercel

1. В настройках проекта перейдите в **Settings** → **Domains**
2. Нажмите **"Add Domain"**
3. Введите: `admin.axondigital.xyz`
4. Нажмите **"Add"**

### 3.2. Настройка DNS записей

Vercel покажет вам DNS записи, которые нужно добавить. Обычно это:

#### Вариант A: CNAME запись (рекомендуется)

```
Type: CNAME
Name: admin
Value: cname.vercel-dns.com
TTL: 3600 (или Auto)
```

#### Вариант B: A запись (если CNAME не поддерживается)

```
Type: A
Name: admin
Value: 76.76.21.21 (IP адрес Vercel, уточните в панели)
TTL: 3600
```

### 3.3. Где добавить DNS записи?

1. Войдите в панель управления вашим доменом `axondigital.xyz`
2. Найдите раздел **DNS Settings** или **DNS Management**
3. Добавьте соответствующую запись (CNAME или A)
4. Сохраните изменения

### 3.4. Ожидание распространения DNS

- Обычно DNS изменения применяются за **5-30 минут**
- Максимальное время: **до 48 часов**
- Проверить можно командой: `dig admin.axondigital.xyz` или `nslookup admin.axondigital.xyz`

### 3.5. Проверка в Vercel

После добавления DNS записи:

1. Вернитесь в Vercel → **Settings** → **Domains**
2. Статус домена должен измениться с **"Pending"** на **"Valid"**
3. Если статус **"Invalid Configuration"**, проверьте DNS записи

---

## 🚀 Шаг 4: Первый деплой

### 4.1. Автоматический деплой

После подключения репозитория:

1. Vercel автоматически запустит первый деплой
2. Вы увидите процесс сборки в реальном времени
3. После успешной сборки проект будет доступен по временному URL: `your-project.vercel.app`

### 4.2. Ручной деплой

Если нужно запустить деплой вручную:

1. Перейдите в **Deployments**
2. Нажмите **"Redeploy"** на последнем деплое
3. Или сделайте push в `main` branch (автоматический деплой)

---

## ✅ Шаг 5: Проверка работы

### 5.1. Проверьте сайт

1. Откройте `https://admin.axondigital.xyz`
2. Убедитесь, что сайт загружается
3. Проверьте авторизацию (если настроена)

### 5.2. Проверьте переменные окружения

Если что-то не работает:

1. Убедитесь, что все переменные окружения добавлены в Vercel
2. Проверьте, что они доступны в Production окружении
3. Пересоберите проект после добавления переменных

---

## 🔄 Шаг 6: Автоматические деплои

### 6.1. Настройка автоматических деплоев

По умолчанию Vercel автоматически деплоит:

- **Production**: при push в `main` branch
- **Preview**: при push в другие branches или создании Pull Request

### 6.2. Отключение автоматических деплоев (опционально)

Если нужно отключить:

1. **Settings** → **Git**
2. Отключите **"Automatic deployments"**

---

## 🛠️ Дополнительные настройки

### Настройка Supabase для production

Убедитесь, что в Supabase Dashboard:

1. **Authentication** → **URL Configuration**:

   **Site URL** (замените `http://localhost:3000` на):

   ```
   https://admin.axondigital.xyz
   ```

   Нажмите **"Save changes"** под полем Site URL.

   **Redirect URLs** (нажмите **"Add URL"** и добавьте):

   ```
   https://admin.axondigital.xyz/auth/callback
   https://admin.axondigital.xyz/**
   ```

   Или используйте wildcard (проще):

   ```
   https://admin.axondigital.xyz/**
   ```

   ⚠️ **Важно**:

   - Для разработки можно оставить `http://localhost:3000` в Site URL, но лучше создать отдельный проект Supabase для production
   - Или использовать wildcard в Redirect URLs: `https://admin.axondigital.xyz/**` (покроет все пути)

2. **API Settings**:
   - Проверьте, что используете правильные ключи (Production, не Development)

### Настройка CORS (если нужно)

Если возникают проблемы с CORS:

1. В Supabase Dashboard → **Settings** → **API**
2. Добавьте в **Allowed Origins**:
   ```
   https://admin.axondigital.xyz
   ```

---

## 📊 Мониторинг и логи

### Просмотр логов

1. В Vercel Dashboard выберите ваш проект
2. Перейдите в **Deployments**
3. Выберите нужный деплой
4. Откройте вкладку **"Logs"** или **"Functions"**

### Аналитика

Vercel автоматически собирает:

- Время загрузки страниц
- Ошибки в production
- Использование функций

---

## 🐛 Решение проблем

### Проблема: Домен не подключается

**Решение:**

1. Проверьте DNS записи (используйте `dig` или `nslookup`)
2. Убедитесь, что прошло достаточно времени (до 48 часов)
3. Проверьте, что домен не используется в другом проекте Vercel

### Проблема: Ошибки сборки

**Решение:**

1. Проверьте логи сборки в Vercel
2. Убедитесь, что все зависимости установлены (`pnpm install`)
3. Проверьте, что все переменные окружения добавлены

### Проблема: Ошибки авторизации

**Решение:**

1. Проверьте настройки Supabase (URL Configuration)
2. Убедитесь, что переменные окружения правильные
3. Проверьте CORS настройки в Supabase

### Проблема: 404 ошибки

**Решение:**

1. Убедитесь, что используется Next.js App Router (не Pages Router)
2. Проверьте, что файлы находятся в правильных директориях
3. Проверьте `next.config.ts` на наличие редиректов

---

## 📝 Чеклист перед деплоем

- [ ] Репозиторий подключен к Vercel
- [ ] Все переменные окружения добавлены
- [ ] DNS записи настроены для `admin.axondigital.xyz`
- [ ] Домен подтвержден в Vercel
- [ ] Supabase URL Configuration обновлена
- [ ] Первый деплой успешно завершен
- [ ] Сайт доступен по `https://admin.axondigital.xyz`
- [ ] Авторизация работает корректно

---

## 🔗 Полезные ссылки

- [Vercel Documentation](https://vercel.com/docs)
- [Next.js Deployment](https://nextjs.org/docs/deployment)
- [Vercel Domains](https://vercel.com/docs/concepts/projects/domains)
- [Environment Variables](https://vercel.com/docs/concepts/projects/environment-variables)

---

**Готово!** Ваш проект должен быть доступен по адресу `https://admin.axondigital.xyz` 🎉
