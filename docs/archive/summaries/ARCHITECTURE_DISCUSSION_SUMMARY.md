# Резюме обсуждения архитектуры

## ✅ Утвержденные решения

### 1. Конфигурация в БД

- ✅ Таблицы `entity_definition` и `field` в БД
- ✅ Миграция данных из `entity-lib/data/` в БД
- ✅ Консистентность данных гарантируется БД

### 2. Старые данные

- ✅ Старые таблицы (blocks, tags) игнорируются
- ✅ Нет миграции старых данных
- ✅ Начинаем с чистого листа

### 3. Загрузка при старте

- ✅ Entities и fields загружаются при старте приложения
- ✅ Используются для формирования бокового меню
- ✅ Кэширование на 5 минут

### 4. Типы связей

- ✅ Поддержка всех типов: many-to-many, many-to-one, one-to-many, one-to-one
- ✅ Поле `relation_type` в `entity_relation`
- ✅ Валидация через триггеры

### 5. Партиционирование

- ✅ По `project_id` для масштабирования
- ✅ Можно включить позже при необходимости

### 6. Функции PostgreSQL

- ✅ Для типичных операций
- ✅ Инкапсуляция сложной логики

### 7. Реализация

- ✅ Новая папка `lib/universal-entity/`
- ✅ Не пересекается со старым кодом

## 💬 Ответы на вопросы

### 1. JSONB = MongoDB в PostgreSQL?

**Ответ:** Частично да, но с преимуществами PostgreSQL.

**Сходства:**

- Гибкая схема
- Документно-ориентированное хранение
- Быстрое чтение целого документа

**Преимущества PostgreSQL:**

- ACID транзакции
- JOIN'ы с другими таблицами
- GIN индексы для JSONB
- Партиционирование
- RLS для безопасности

**Вывод:** Гибкость NoSQL + надежность SQL = оптимальный баланс.

### 2. Generated columns - что выигрываем?

**Ответ:** В нашем случае выигрыш минимальный.

**Проблема:** Generated columns работают только если все значения одного типа в одной колонке. У нас каждое поле может быть разного типа.

**Альтернатива:** JSONB с индексами на конкретные поля:

```sql
CREATE INDEX idx_entity_instance_name
  ON entity_instance((data->>'name'))
  WHERE data ? 'name' AND entity_definition_id = 'blocks-id';
```

**Вывод:** Используем JSONB с индексами на популярные поля, generated columns не нужны.

### 3. Гибридный подход - не оправдан?

**Ответ:** Да, не оправдан.

**Проблемы:**

1. Если поиск по `title`, а `title` не основное поле - все равно нужно искать в JSONB
2. Нет универсальности
3. При добавлении нового "основного" поля нужна миграция
4. Сложнее логика

**Вывод:** JSONB подход проще и универсальнее.

### 4. Популярные поля в основной таблице?

**Ответ:** Нет, лучше индексы на JSONB поля.

**Проблемы добавления колонок:**

- Нарушает универсальность
- Нужна миграция при добавлении
- Дублирование данных
- Сложнее синхронизация

**Решение:** JSONB с индексами:

```sql
-- Индекс на конкретное поле
CREATE INDEX idx_entity_instance_name
  ON entity_instance((data->>'name'))
  WHERE data ? 'name' AND entity_definition_id = 'blocks-id';
```

**Вывод:** JSONB + индексы на нужные поля = производительность без потери универсальности.

### 5. Фильтрация по полям

**Решение:** Индексы на JSONB поля + полнотекстовый поиск.

```sql
-- Простой поиск
WHERE data->>'name' ILIKE '%search%'

-- С индексом
CREATE INDEX idx_entity_instance_name_search
  ON entity_instance USING GIN ((data->>'name') gin_trgm_ops);

-- Полнотекстовый поиск
CREATE INDEX idx_entity_instance_fulltext
  ON entity_instance USING GIN (to_tsvector('english', data::text));
```

## 🎯 Финальный подход: JSONB с оптимизацией

### Структура

```sql
CREATE TABLE entity_instance (
  id UUID PRIMARY KEY,
  entity_definition_id UUID NOT NULL,
  project_id UUID NOT NULL,
  data JSONB NOT NULL DEFAULT '{}'::jsonb, -- все поля здесь
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

### Преимущества

1. ✅ **Универсальность** - любая структура без миграций
2. ✅ **Производительность** - индексы на нужных полях
3. ✅ **Простота** - один запрос для получения всех данных
4. ✅ **Гибкость** - легко добавлять новые поля
5. ✅ **Масштабируемость** - партиционирование по project_id

### Индексы

```sql
-- GIN индекс для общего поиска
CREATE INDEX idx_entity_instance_data_gin
  ON entity_instance USING GIN (data);

-- Индексы на популярные поля (создаются по необходимости)
CREATE INDEX idx_entity_instance_name
  ON entity_instance((data->>'name'))
  WHERE data ? 'name' AND entity_definition_id = 'blocks-id';

-- Полнотекстовый поиск
CREATE INDEX idx_entity_instance_fulltext
  ON entity_instance USING GIN (to_tsvector('english', data::text));
```

## 📊 Сравнение подходов

| Критерий           | JSONB            | EAV (fieldValue) | Гибридный  |
| ------------------ | ---------------- | ---------------- | ---------- |
| Универсальность    | ✅ Очень высокая | ✅ Высокая       | ⚠️ Средняя |
| Производительность | ✅ Хорошая       | ⚠️ Средняя       | ✅ Хорошая |
| Простота запросов  | ✅ Простые       | ❌ Сложные       | ⚠️ Средние |
| Масштабируемость   | ✅ Высокая       | ✅ Высокая       | ⚠️ Средняя |
| Гибкость           | ✅ Максимальная  | ✅ Высокая       | ❌ Низкая  |

**Вывод:** JSONB подход оптимален для наших требований.

## 🚀 План реализации

### Фаза 1: Таблицы и миграция (1-2 дня)

- ✅ Создать таблицы `entity_definition`, `field`
- ✅ Мигрировать данные из файлов
- ✅ Создать таблицы `entity_instance`, `entity_relation`
- ✅ Настроить индексы и RLS

### Фаза 2: Сервисный слой (2-3 дня)

- ✅ `config-service.ts` - загрузка конфигурации
- ✅ `instance-service.ts` - CRUD экземпляров
- ✅ `relation-service.ts` - работа со связями
- ✅ Типы TypeScript

### Фаза 3: Интеграция (2-3 дня)

- ✅ Загрузка конфигурации в layout
- ✅ Формирование меню из entities
- ✅ Тестирование

### Фаза 4: UI слой (3-5 дней)

- ✅ Универсальный список
- ✅ Универсальная форма
- ✅ Динамическая генерация страниц

## 📁 Структура проекта

```
lib/
  universal-entity/          # Новая папка
    ├── types.ts            # TypeScript типы
    ├── config-service.ts   # Загрузка конфигурации
    ├── instance-service.ts # Работа с экземплярами
    ├── relation-service.ts # Работа со связями
    └── index.ts            # Экспорты
```

## ✅ Итоговые решения

1. **JSONB подход** - основной способ хранения
2. **Индексы на популярные поля** - для производительности
3. **Партиционирование** - по project_id (опционально)
4. **Функции PostgreSQL** - для сложных операций
5. **Конфигурация в БД** - для консистентности
6. **Загрузка при старте** - для формирования меню
7. **Все типы связей** - many-to-many, many-to-one, one-to-many, one-to-one
8. **Новая папка** - `lib/universal-entity/`
9. **Без миграции старых данных** - начинаем с чистого листа

## 🎯 Следующие шаги

1. Выполнить SQL миграцию
2. Создать сервисный слой
3. Интегрировать загрузку конфигурации
4. Создать UI компоненты
5. Тестирование
