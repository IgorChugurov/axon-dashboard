# Рекомендации по очистке неиспользуемых файлов

## ❌ Файлы/папки для удаления

### 1. Пустые папки в app/api/auth/

Все три папки пустые (не содержат route.ts):

- ❌ `app/api/auth/clear-auth/` - пустая папка
- ❌ `app/api/auth/set-tokens/` - пустая папка
- ❌ `app/api/auth/refresh/` - пустая папка (есть `refresh-token/`)

**Причина:** Эти папки были созданы ранее, но не используются. Функциональность реализована в других местах:

- `clear-auth` → логика в `lib/auth/utils.ts` (clearAuthCookies)
- `set-tokens` → логика в `lib/auth/utils.ts` (setAccessTokenCookie, setRefreshTokenCookie)
- `refresh` → есть `refresh-token/` который используется

### 2. Компонент AuthErrorHandler

- ❌ `components/AuthErrorHandler.tsx` - не используется

**Причина:**

- Ранее использовался в `app/page.tsx` для показа ошибки авторизации
- Теперь редирект делается через `redirect("/login")` в Server Component
- Компонент больше не импортируется нигде

**Рекомендация:** Можно удалить или оставить для будущего использования (если планируется показывать ошибки на клиенте)

## ✅ Что оставить (используется)

### lib/

- ✅ `lib/api.ts` - используется в `AuthProvider` для клиентских API запросов
- ✅ `lib/utils.ts` - используется в UI компонентах (функция `cn()` для классов)
- ✅ `lib/api/handlers.ts` - основная логика работы с бэкендом
- ✅ `lib/api/server.ts` - функции для Server Components
- ✅ `lib/auth/utils.ts` - работа с cookies
- ✅ `lib/auth/types.ts` - типы для авторизации
- ✅ `lib/projects/server.ts` - провайдер для проектов
- ✅ `lib/projects/types.ts` - типы проектов
- ✅ `lib/server-data/base.ts` - базовый класс провайдеров
- ✅ `lib/server-data/types.ts` - типы для серверных данных

### app/api/

- ✅ `app/api/auth/login/route.ts` - авторизация
- ✅ `app/api/auth/logout/route.ts` - выход
- ✅ `app/api/auth/me/route.ts` - данные пользователя
- ✅ `app/api/auth/refresh-token/route.ts` - обновление токенов
- ✅ `app/api/[entity]/route.ts` - универсальный API для сущностей

### components/

- ✅ Все компоненты в `components/ui/` - используются
- ✅ `components/providers/AuthProvider.tsx` - используется
- ✅ `components/providers/ThemeProvider.tsx` - используется
- ✅ `components/ProjectsList.tsx` - используется
- ✅ `components/Navbar.tsx` - используется
- ✅ `components/AppSidebar.tsx` - используется
- ✅ `components/Logo.tsx` - используется

## 📊 Статистика

### Используется:

- ✅ 9 файлов в `lib/`
- ✅ 5 route handlers
- ✅ 8+ компонентов
- ✅ 1 middleware

### Не используется:

- ❌ 3 пустые папки
- ❌ 1 компонент (AuthErrorHandler)

## 🧹 Команды для очистки

```bash
# Удалить пустые папки
rm -rf app/api/auth/clear-auth
rm -rf app/api/auth/set-tokens
rm -rf app/api/auth/refresh

# Удалить неиспользуемый компонент (опционально)
rm components/AuthErrorHandler.tsx
```

## ⚠️ Внимание

Перед удалением убедитесь:

1. Что папки действительно пустые (проверено ✅)
2. Что `AuthErrorHandler` не используется (проверено ✅)
3. Что нет ссылок на удаляемые файлы в коде (проверено ✅)
