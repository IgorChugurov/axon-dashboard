# План рефакторинга UniversalEntityList

**Дата создания:** 2025-01-20  
**Версия:** 1.0

---

## 📋 Содержание

1. [Цель рефакторинга](#цель-рефакторинга)
2. [Анализ требований](#анализ-требований)
3. [Альтернативные подходы](#альтернативные-подходы)
4. [Выбранный подход](#выбранный-подход)
5. [Слабые места и риски](#слабые-места-и-риски)
6. [Анализ зависимостей](#анализ-зависимостей)
7. [Детальный план реализации](#детальный-план-реализации)
8. [Ожидаемые результаты](#ожидаемые-результаты)

---

## Цель рефакторинга

Привести `UniversalEntityList` к единообразному виду с универсальными формами:

1. **Использование сервисов** - компонент должен принимать сервис или функцию загрузки данных
2. **Работающая пагинация** - корректная загрузка данных при изменении страницы
3. **Работающий поиск** - загрузка данных при изменении поискового запроса
4. **Единообразие** - все страницы используют один подход
5. **Конфигурация из EntityUIConfig** - все настройки из конфига, без хардкода

---

## Анализ требований

### Функциональные требования

1. **Загрузка данных через сервисы:**

   - Компонент должен принимать функцию/сервис для загрузки данных
   - Поддержка различных типов сервисов (entity-service, instance-service, custom services)
   - Обработка ошибок загрузки

2. **Пагинация:**

   - Если `enablePagination: true` - использовать пагинацию
   - Если `enablePagination: false` - загружать все данные сразу
   - Отображение информации о текущей странице и общем количестве
   - Кнопки Previous/Next с правильными состояниями disabled

3. **Поиск:**

   - Если `showSearch: true` - показывать поле поиска
   - Использовать `searchableFields` из конфига или автоматически генерировать из полей с `searchable: true`
   - Debounce для поиска (опционально)
   - Очистка поиска

4. **Таблица:**

   - Формирование колонок из `uiConfig.list.columns`
   - Поддержка всех типов колонок: `text`, `date`, `number`, `boolean`, `naigateToDetails`, `openEditPage`, `actions`
   - Правильное форматирование значений
   - Actions колонка с поддержкой всех типов действий

5. **Фильтры (будущая реализация):**
   - Структура готова в конфиге
   - Пока не реализуем, но оставляем место для будущей реализации

### Нефункциональные требования

1. **Производительность:**

   - Минимизация перезагрузок данных
   - Оптимистичные обновления где возможно
   - Кэширование данных (опционально)

2. **UX:**

   - Индикаторы загрузки
   - Плавные переходы
   - Обработка ошибок с понятными сообщениями

3. **Поддерживаемость:**
   - Единообразный код
   - Минимум хардкода
   - Легко расширяемый

---

## Альтернативные подходы

### Подход 1: Передача функции загрузки данных

**Описание:** Компонент принимает функцию `onLoadData(page, search)` которая возвращает данные.

**Плюсы:**

- Гибкость - каждая страница может использовать свой сервис
- Простота реализации
- Легко тестировать

**Минусы:**

- Нужно передавать функцию в каждой странице
- Нет единообразия в использовании сервисов

**Пример:**

```typescript
<UniversalEntityList
  entityDefinition={entityDefinition}
  uiConfig={uiConfig}
  onLoadData={async (page, search) => {
    return await getEnvironments(projectId, { page, limit: 20, search });
  }}
  projectId={projectId}
/>
```

---

### Подход 2: Передача сервиса как объекта

**Описание:** Компонент принимает объект сервиса с методом `getAll(params)`.

**Плюсы:**

- Единообразный интерфейс для всех сервисов
- Легко расширять функциональность
- Можно добавить кэширование на уровне сервиса

**Минусы:**

- Нужно адаптировать существующие сервисы к единому интерфейсу
- Более сложная реализация

**Пример:**

```typescript
const environmentService = {
  getAll: async (params) => {
    return await getEnvironments(projectId, params);
  },
};

<UniversalEntityList
  entityDefinition={entityDefinition}
  uiConfig={uiConfig}
  service={environmentService}
  projectId={projectId}
/>;
```

---

### Подход 3: Автоматическое определение сервиса по типу сущности

**Описание:** Компонент сам определяет какой сервис использовать на основе `entityDefinition.tableName`.

**Плюсы:**

- Не нужно передавать сервис в каждой странице
- Автоматическая интеграция

**Минусы:**

- Хардкод логики выбора сервиса
- Сложно расширять для новых типов сущностей
- Нарушает принцип инверсии зависимостей

**Пример:**

```typescript
// Внутри компонента
if (entityDefinition.tableName === "environments") {
  service = environmentService;
} else if (entityDefinition.tableName === "entity_definition") {
  service = entityDefinitionService;
}
```

---

### Подход 4: Использование API routes

**Описание:** Компонент делает запросы к API routes (`/api/entity-instances/[id]`, `/api/environments`, etc.).

**Плюсы:**

- Единообразный подход через HTTP
- Легко кэшировать на уровне Next.js

**Минусы:**

- Дополнительный HTTP слой
- Медленнее чем прямые вызовы
- Нужно создавать API routes для всех типов сущностей

---

## Выбранный подход

**Выбран: Подход 1 (Передача функции загрузки данных) + Подход 2 (Сервисы с единым интерфейсом)**

### Обоснование

1. **Гибкость:** Каждая страница может использовать свой сервис или функцию
2. **Единообразие:** Создадим обёртки для сервисов с единым интерфейсом
3. **Простота:** Не нужно хардкодить логику выбора сервиса
4. **Расширяемость:** Легко добавить новые типы сервисов

### Архитектура

1. **Универсальный интерфейс сервиса:**

```typescript
interface ListService {
  getAll(params: {
    page: number;
    limit: number;
    search?: string;
    filters?: any[];
  }): Promise<{
    data: any[];
    pagination: {
      page: number;
      limit: number;
      total: number;
      totalPages: number;
      hasPreviousPage: boolean;
      hasNextPage: boolean;
    };
  }>;
}
```

2. **Обёртки для существующих сервисов:**

- `createEntityServiceAdapter()` - для entity-service
- `createInstanceServiceAdapter()` - для instance-service
- `createEnvironmentServiceAdapter()` - для environment-service
- `createConfigServiceAdapter()` - для config-service (EntityDefinitions, Fields)

3. **Компонент принимает функцию:**

```typescript
interface UniversalEntityListProps {
  entityDefinition: EntityDefinition;
  uiConfig: EntityUIConfig;
  onLoadData: (params: LoadDataParams) => Promise<LoadDataResponse>;
  projectId: string;
  fields?: Field[];
}
```

---

## Слабые места и риски

### 1. Миграция существующих страниц

**Риск:** Нужно обновить все 4 страницы со списками.

**Митигация:**

- Создаём обёртки для сервисов заранее
- Мигрируем по одной странице
- Тестируем каждую миграцию

### 2. Разные форматы данных от сервисов

**Риск:** Разные сервисы возвращают данные в разных форматах.

**Митигация:**

- Создаём адаптеры для каждого сервиса
- Нормализуем данные в единый формат

### 3. Производительность при частых перезагрузках

**Риск:** При быстром вводе поиска или переключении страниц может быть много запросов.

**Митигация:**

- Добавляем debounce для поиска (300ms)
- Используем `useTransition` для плавных переходов
- Кэшируем данные на уровне компонента (опционально)

### 4. Обработка ошибок

**Риск:** Ошибки загрузки данных могут сломать UI.

**Митигация:**

- Добавляем обработку ошибок с понятными сообщениями
- Показываем fallback UI при ошибках
- Логируем ошибки для отладки

### 5. Роутинг для разных типов сущностей

**Риск:** Разные типы сущностей имеют разные паттерны роутинга.

**Митигация:**

- Выносим логику роутинга в утилиты
- Используем конфигурацию из `EntityDefinition` где возможно
- Создаём универсальные функции для генерации URL

---

## Анализ зависимостей

### Файлы, которые нужно изменить

1. **`components/UniversalEntityList.tsx`**

   - Полная переработка компонента
   - Добавление загрузки данных через сервисы
   - Исправление пагинации и поиска

2. **`app/projects/[projectId]/page.tsx`**

   - Создание адаптера для `getEntityDefinitions`
   - Передача функции загрузки в компонент

3. **`app/projects/[projectId]/entity-instances/[entityDefinitionId]/page.tsx`**

   - Создание адаптера для `getInstances`
   - Миграция с `EntityListClient` на `UniversalEntityList`

4. **`app/projects/[projectId]/entity-definition/[entityDefinitionId]/fields/page.tsx`**

   - Создание адаптера для `getEntityDefinitionWithFields`
   - Передача функции загрузки в компонент

5. **`app/projects/[projectId]/settings/EnvironmentsTab.tsx`**

   - Создание адаптера для `getEnvironments`
   - Обновление использования компонента

6. **`app/projects/[projectId]/entity-instances/[entityDefinitionId]/EntityListClient.tsx`**
   - Удаление (мигрируем на `UniversalEntityList`)

### Новые файлы

1. **`lib/universal-entity/list-service-adapters.ts`**

   - Адаптеры для всех типов сервисов
   - Утилиты для нормализации данных

2. **`lib/universal-entity/list-routing-utils.ts`**
   - Утилиты для генерации URL для разных типов сущностей
   - Универсальные функции навигации

### Зависимости, которые не изменятся

- `lib/universal-entity/types.ts` - типы остаются без изменений
- `lib/universal-entity/ui-config-types.ts` - типы остаются без изменений
- `lib/entity-service/base.ts` - сервис остаётся без изменений
- `lib/universal-entity/instance-service.ts` - сервис остаётся без изменений
- `lib/environments/service.ts` - сервис остаётся без изменений
- `lib/universal-entity/config-service.ts` - сервис остаётся без изменений

---

## Детальный план реализации

### Этап 1: Создание интерфейсов и адаптеров

**Цель:** Создать единый интерфейс для загрузки данных и адаптеры для существующих сервисов.

**Задачи:**

1. Создать файл `lib/universal-entity/list-service-adapters.ts`:

   - Определить интерфейс `ListService`
   - Создать `createEntityServiceAdapter()` для entity-service
   - Создать `createInstanceServiceAdapter()` для instance-service
   - Создать `createEnvironmentServiceAdapter()` для environment-service
   - Создать `createConfigServiceAdapter()` для config-service

2. Создать файл `lib/universal-entity/list-routing-utils.ts`:
   - Функция `getCreateUrl(entityDefinition, projectId)`
   - Функция `getEditUrl(entityDefinition, instanceId, projectId)`
   - Функция `getDetailsUrl(entityDefinition, instanceId, projectId)`
   - Функция `getLinkUrl(entityDefinition, instanceId, additionalUrl, projectId)`

**Ожидаемый результат:**

- Единый интерфейс для всех сервисов
- Адаптеры для всех существующих сервисов
- Утилиты для роутинга

---

### Этап 2: Рефакторинг UniversalEntityList

**Цель:** Переработать компонент для использования сервисов и правильной работы пагинации/поиска.

**Задачи:**

1. **Обновить интерфейс компонента:**

   ```typescript
   interface UniversalEntityListProps {
     entityDefinition: EntityDefinition;
     uiConfig: EntityUIConfig;
     onLoadData: (params: {
       page: number;
       limit: number;
       search?: string;
     }) => Promise<{
       data: any[];
       pagination: {
         page: number;
         limit: number;
         total: number;
         totalPages: number;
         hasPreviousPage: boolean;
         hasNextPage: boolean;
       };
     }>;
     projectId: string;
     fields?: Field[];
   }
   ```

2. **Добавить состояние для данных:**

   - `instances` - текущие данные
   - `loading` - состояние загрузки
   - `error` - ошибка загрузки
   - `pagination` - информация о пагинации

3. **Реализовать загрузку данных:**

   - Функция `loadData(page, search)` которая вызывает `onLoadData`
   - Использовать `useEffect` для загрузки при изменении page/search
   - Обработка ошибок

4. **Исправить пагинацию:**

   - Использовать `pagination.hasNextPage` вместо проверки `instances.length`
   - Правильные состояния disabled для кнопок
   - Отображение информации о странице

5. **Исправить поиск:**

   - Debounce для поиска (300ms)
   - Загрузка данных при изменении поиска
   - Сброс на первую страницу при поиске

6. **Вынести логику роутинга:**

   - Использовать утилиты из `list-routing-utils.ts`
   - Убрать хардкод условий для разных типов сущностей

7. **Добавить индикаторы загрузки:**

   - Skeleton при загрузке
   - Индикатор в кнопках пагинации

8. **Обработка ошибок:**
   - Отображение ошибок с понятными сообщениями
   - Кнопка "Повторить" при ошибке

**Ожидаемый результат:**

- Компонент работает с сервисами
- Пагинация работает корректно
- Поиск работает корректно
- Нет хардкода роутинга

---

### Этап 3: Миграция страницы Entity Definitions

**Цель:** Мигрировать страницу списка Entity Definitions на новый подход.

**Задачи:**

1. Создать адаптер для `getEntityDefinitions`:

   ```typescript
   const entityDefinitionsService = createConfigServiceAdapter(
     async (params) => {
       const definitions = await getEntityDefinitions(projectId);
       // Нормализация данных и пагинация
       return normalizeAndPaginate(definitions, params);
     }
   );
   ```

2. Обновить страницу:

   - Убрать передачу `initialInstances`
   - Передать функцию `onLoadData`
   - Убрать создание фиктивной `EntityDefinition` (использовать реальную)

3. Протестировать:
   - Пагинация работает
   - Поиск работает
   - Навигация работает

**Ожидаемый результат:**

- Страница использует новый подход
- Все функции работают корректно

---

### Этап 4: Миграция страницы Entity Instances

**Цель:** Мигрировать страницу списка Entity Instances с `EntityListClient` на `UniversalEntityList`.

**Задачи:**

1. Создать адаптер для `getInstances`:

   ```typescript
   const instancesService = createInstanceServiceAdapter(
     entityDefinitionId,
     projectId,
     fields
   );
   ```

2. Обновить страницу:

   - Заменить `EntityListClient` на `UniversalEntityList`
   - Передать функцию загрузки через адаптер
   - Загрузить `uiConfig` через `generateUIConfig()`

3. Удалить `EntityListClient.tsx`:

   - Компонент больше не нужен

4. Протестировать:
   - Пагинация работает
   - Поиск работает
   - Отображение связей работает
   - Навигация работает

**Ожидаемый результат:**

- Страница использует `UniversalEntityList`
- Все функции работают корректно
- `EntityListClient` удалён

---

### Этап 5: Миграция страницы Fields

**Цель:** Мигрировать страницу списка Fields на новый подход.

**Задачи:**

1. Создать адаптер для `getEntityDefinitionWithFields`:

   ```typescript
   const fieldsService = createConfigServiceAdapter(async (params) => {
     const { fields } = await getEntityDefinitionWithFields(entityDefinitionId);
     // Нормализация и пагинация
     return normalizeAndPaginate(fields, params);
   });
   ```

2. Обновить страницу:

   - Убрать передачу `initialInstances`
   - Передать функцию `onLoadData`
   - Убрать создание фиктивной `EntityDefinition`

3. Протестировать:
   - Пагинация работает
   - Поиск работает
   - Навигация работает

**Ожидаемый результат:**

- Страница использует новый подход
- Все функции работают корректно

---

### Этап 6: Миграция страницы Environments

**Цель:** Обновить страницу Environments для правильной работы пагинации и поиска.

**Задачи:**

1. Создать адаптер для `getEnvironments`:

   ```typescript
   const environmentsService = createEnvironmentServiceAdapter(projectId);
   ```

2. Обновить `EnvironmentsTab`:

   - Убрать передачу `initialInstances`
   - Передать функцию `onLoadData`
   - Убрать создание фиктивной `EntityDefinition` и `Field[]` (использовать из конфига)

3. Протестировать:
   - Пагинация работает
   - Поиск работает
   - Навигация работает

**Ожидаемый результат:**

- Страница использует новый подход
- Все функции работают корректно

---

### Этап 7: Финальная проверка и оптимизация

**Цель:** Убедиться что всё работает и оптимизировать производительность.

**Задачи:**

1. **Тестирование всех страниц:**

   - Entity Definitions - пагинация, поиск, навигация
   - Entity Instances - пагинация, поиск, навигация, связи
   - Fields - пагинация, поиск, навигация
   - Environments - пагинация, поиск, навигация

2. **Оптимизация:**

   - Добавить debounce для поиска (300ms)
   - Проверить производительность при больших объёмах данных
   - Оптимизировать перерисовки

3. **Обработка edge cases:**

   - Пустые списки
   - Ошибки загрузки
   - Нет данных после поиска
   - Очень большие списки

4. **Документация:**
   - Обновить `LISTS_STRUCTURE.md`
   - Добавить примеры использования

**Ожидаемый результат:**

- Все страницы работают корректно
- Производительность оптимизирована
- Документация обновлена

---

## Ожидаемые результаты

### После рефакторинга

1. **Единообразие:**

   - Все страницы используют `UniversalEntityList`
   - Единый подход к загрузке данных
   - Единый подход к роутингу

2. **Работающие функции:**

   - ✅ Пагинация работает корректно
   - ✅ Поиск работает корректно
   - ✅ Навигация работает корректно
   - ✅ Обработка ошибок

3. **Поддерживаемость:**

   - Нет хардкода
   - Легко добавлять новые типы сущностей
   - Легко расширять функциональность

4. **Производительность:**
   - Оптимизированная загрузка данных
   - Debounce для поиска
   - Плавные переходы

### Метрики успеха

- ✅ Все 4 страницы используют `UniversalEntityList`
- ✅ Пагинация работает на всех страницах
- ✅ Поиск работает на всех страницах
- ✅ Нет дублирования кода
- ✅ Нет хардкода роутинга
- ✅ Все тесты проходят

---

## Примечания

1. **Фильтры:** Не реализуем в этом рефакторинге, но оставляем структуру для будущей реализации.

2. **Сортировка:** Не реализуем в этом рефакторинге, но структура готова в конфиге.

3. **Кэширование:** Опционально, можно добавить позже если нужно.

4. **Тестирование:** После каждого этапа нужно тестировать функциональность.
