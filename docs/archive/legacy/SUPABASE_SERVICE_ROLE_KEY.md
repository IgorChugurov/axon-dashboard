# SUPABASE_SERVICE_ROLE_KEY - Где взять и зачем нужен

## 🔍 Где найти Service Role Key

1. Откройте **Supabase Dashboard** → ваш проект
2. Перейдите в **Settings** (⚙️ в левом меню)
3. Выберите **API** в списке настроек
4. Найдите раздел **"Project API keys"**
5. Там будет два ключа:
   - **anon public** - публичный ключ (безопасен для клиента)
   - **service_role** - секретный ключ (⚠️ только для сервера!)

6. Нажмите на кнопку **"Reveal"** или **"Copy"** рядом с **service_role** ключом
7. Скопируйте ключ (он начинается с `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`)

---

## ⚠️ ВАЖНО: Безопасность

**Service Role Key имеет полный доступ к вашей базе данных и обходит все RLS политики!**

- ✅ Используйте **только на сервере** (в API routes, Server Actions, Server Components)
- ❌ **НИКОГДА** не используйте на клиенте (Client Components, браузер)
- ❌ **НИКОГДА** не коммитьте в Git
- ❌ **НИКОГДА** не добавляйте в `NEXT_PUBLIC_*` переменные

---

## 🎯 Зачем нужен Service Role Key?

Service Role Key используется для **специальных операций**, которые требуют обхода RLS (Row Level Security):

### Примеры использования:

1. **Админские операции**
   - Создание пользователей администратором
   - Изменение данных любых пользователей
   - Массовые операции

2. **Server-side операции**
   - Миграция данных
   - Создание профилей при регистрации (если не через триггер)
   - Административные задачи

3. **Backend интеграции**
   - Webhooks обработка
   - Cron jobs
   - Внешние сервисы

---

## 📝 Для базовой авторизации НЕ обязателен!

**Хорошая новость**: Для обычной авторизации (вход, выход, получение пользователя) **Service Role Key НЕ нужен!**

Достаточно:
- ✅ `NEXT_PUBLIC_SUPABASE_URL`
- ✅ `NEXT_PUBLIC_SUPABASE_ANON_KEY`

Этих ключей хватит для:
- OAuth авторизации (Google, GitHub)
- Email/Password авторизации
- Получения данных пользователя
- Защиты маршрутов через middleware

---

## 🔧 Когда понадобится Service Role Key?

Service Role Key понадобится, если вы хотите:

1. **Создавать пользователей программно** (не через Supabase Auth UI)
2. **Выполнять админские операции** (изменять данные любых пользователей)
3. **Использовать Admin API** для специальных задач

---

## 💡 Рекомендация

Для начала работы:

1. **Можно оставить `SUPABASE_SERVICE_ROLE_KEY` пустым** - базовая авторизация будет работать
2. **Добавьте его позже**, когда понадобятся админские функции

Если вы не используете `createAdminClient()` в коде, то Service Role Key не обязателен.

---

## 📋 Где используется в нашем коде?

Service Role Key используется только в:
- `lib/supabase/admin.ts` - функция `createAdminClient()`

**Текущая реализация**: Мы создали admin клиент, но **не используем его** в базовой авторизации. Он нужен только для будущих админских функций.

---

## ✅ Итог

**Для начала работы достаточно:**
```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
```

**Service Role Key можно добавить позже**, когда понадобятся админские функции.

