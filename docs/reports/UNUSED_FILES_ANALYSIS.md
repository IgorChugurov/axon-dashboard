# Анализ неиспользуемых файлов

**Дата:** 2025-01-30  
**Версия:** 1.0

---

## 📋 Резюме

Проведен анализ проекта на наличие неиспользуемых файлов. Найдено несколько файлов, которые можно удалить или переместить.

---

## ❌ Файлы для удаления

### 1. `app/projects/[projectId]/settings/ProjectSettingsTab.tsx`

**Статус:** ❌ Не используется

**Причина:**

- Заглушка с текстом "Project settings will be implemented here"
- Не импортируется нигде в проекте
- Страница настроек проекта (`app/projects/[projectId]/settings/page.tsx`) использует `ProjectFormNew` напрямую

**Размер:** 22 строки

**Рекомендация:** ✅ Удалить

---

### 2. `app/projects/[projectId]/[entityDefId]/entity-definition-actions.ts`

**Статус:** ❌ Не используется

**Причина:**

- Старые Server Actions для EntityDefinition
- Не импортируется нигде в проекте
- Заменены на клиентские сервисы (`entity-definition-client-service.ts`)
- Все формы используют `EntityDefinitionFormNew`, который использует клиентские сервисы

**Содержимое:**

- `createEntityDefinitionAction`
- `updateEntityDefinitionAction`
- `deleteEntityDefinitionAction`

**Размер:** 189 строк

**Рекомендация:** ✅ Удалить

---

## ⚠️ Файлы, которые используются, но находятся в неправильном месте

### 1. `app/projects/[projectId]/[entityDefId]/RelationSelect.tsx`

**Статус:** ✅ Используется, но в неправильном месте

**Используется в:**

- `lib/form-generation/components/inputs-shadcn/InputRelationShadcn.tsx`
- `lib/form-generation/components/inputs/InputRelation.tsx`

**Проблема:**

- Компонент находится в `app/`, но это не страница
- Должен быть в `components/`

**Рекомендация:** ⚠️ Переместить в `components/universal-entity/RelationSelect.tsx` или `components/form-generation/RelationSelect.tsx`

---

## ✅ Файлы, которые используются (не удалять)

### 1. `app/[projectId]/page.tsx` и `app/[projectId]/[...rest]/page.tsx`

**Статус:** ✅ Используются (редиректы)

**Причина:**

- Редиректы со старых путей на новые
- Нужны для обратной совместимости
- Не удалять

---

## 📊 Статистика

| Категория             | Количество | Строк кода |
| --------------------- | ---------- | ---------- |
| Файлы для удаления    | 2          | 211        |
| Файлы для перемещения | 1          | 160        |
| Всего можно очистить  | 3          | 371        |

---

## 🎯 Рекомендации

### Немедленные действия

1. ✅ Удалить `ProjectSettingsTab.tsx` - не используется
2. ✅ Удалить `entity-definition-actions.ts` - заменен на клиентские сервисы

### Долгосрочные действия

1. ⚠️ Переместить `RelationSelect.tsx` в правильное место (`components/`)
2. ⚠️ Обновить импорты после перемещения

---

## ✅ Выводы

Найдено **2 файла** для немедленного удаления (211 строк кода) и **1 файл** для перемещения в правильное место. После очистки кодовая база станет чище и проще в поддержке.
