# План реализации системы администрирования

## 🎯 Цель

Реализовать систему ролей и управления админами проектов в админ-панели.

---

## 📋 Краткий план реализации

### Этап 1: База данных (миграции)

1. **Создать `project_admin_roles`**

   - `projectSuperAdmin` - суперадмин проекта
   - `projectAdmin` - админ проекта

2. **Создать `project_admins`**

   - Связь: `project_id` + `user_id` + `role_id`
   - RLS политики: только superAdmin и projectSuperAdmin могут управлять

3. **Обновить функции проверки ролей**
   - `is_project_super_admin(project_id, user_id)`
   - `is_project_admin(project_id, user_id)`
   - `get_user_project_role(project_id, user_id)`

### Этап 2: Backend (сервисы)

1. **Обновить `role-service.ts`**

   - Добавить функции для проектных ролей
   - Поддержка проверки ролей на уровне проекта

2. **Создать `project-admin-service.ts`**

   - CRUD операции для `project_admins`
   - Проверка прав доступа

3. **Обновить middleware**
   - Проверка проектных ролей
   - Определение доступных проектов для пользователя

### Этап 3: API endpoints

1. **`/api/projects/[projectId]/admins`**

   - GET - список админов проекта
   - POST - добавить админа проекта
   - DELETE - удалить админа проекта
   - PATCH - изменить роль админа

2. **Проверка прав в каждом endpoint**
   - Только superAdmin или projectSuperAdmin могут управлять

### Этап 4: Frontend (UI)

1. **Страница управления админами проекта**

   - `/projects/[projectId]/admins`
   - Список админов проекта
   - Форма добавления админа
   - Удаление/изменение роли

2. **Обновить навигацию**
   - Показывать только доступные проекты
   - Скрыть недоступные функции

---

## 🔄 Флоу работы системы

### Флоу 1: Авторизация и определение роли

```
1. Пользователь логинится → Supabase Auth
   ↓
2. Middleware проверяет роль:
   ├─ superAdmin → доступ ко всем проектам
   ├─ projectSuperAdmin → доступ к своим проектам
   └─ projectAdmin → доступ к своим проектам (ограниченный)
   ↓
3. Определение доступных проектов:
   ├─ superAdmin → все проекты (автоматически, НЕ из project_admins)
   └─ projectSuperAdmin/projectAdmin → только проекты из project_admins
   ↓
4. Установка контекста проекта
```

### Флоу 2: SuperAdmin создает projectSuperAdmin

```
1. SuperAdmin открывает страницу админов проекта
   ↓
2. Вводит email пользователя
   ↓
3. Система проверяет:
   ├─ Пользователь существует в auth.users?
   ├─ Уже является админом проекта?
   └─ SuperAdmin имеет права?
   ↓
4. Создание записи в project_admins:
   - project_id
   - user_id (из auth.users)
   - role_id (projectSuperAdmin)
   ↓
5. Пользователь получает доступ к проекту
```

### Флоу 3: ProjectSuperAdmin создает projectAdmin

```
1. ProjectSuperAdmin открывает страницу админов проекта
   ↓
2. Вводит email пользователя
   ↓
3. Система проверяет:
   ├─ Пользователь существует в auth.users?
   ├─ ProjectSuperAdmin имеет права для этого проекта?
   └─ Пользователь уже админ?
   ↓
4. Создание записи в project_admins:
   - project_id (текущий проект)
   - user_id
   - role_id (projectAdmin)
   ↓
5. Пользователь получает ограниченный доступ к проекту
```

### Флоу 4: Доступ к данным проекта

```
1. Пользователь запрашивает данные проекта
   ↓
2. Проверка доступа:
   ├─ superAdmin → ✅ доступ
   ├─ projectSuperAdmin → ✅ доступ (только свой проект)
   └─ projectAdmin → ✅ доступ (только свой проект, ограниченный)
   ↓
3. Фильтрация данных:
   ├─ superAdmin → все данные проекта
   ├─ projectSuperAdmin → все данные проекта
   └─ projectAdmin → только универсальные сущности (не entity_definition)
   ↓
4. Возврат данных
```

### Флоу 5: Управление структурой данных (entity_definition)

```
1. Пользователь пытается создать/изменить entity_definition
   ↓
2. Проверка прав:
   ├─ superAdmin → ✅ доступ
   ├─ projectSuperAdmin → ✅ доступ (только свой проект)
   └─ projectAdmin → ❌ доступ запрещен
   ↓
3. Выполнение операции или ошибка доступа
```

### Флоу 6: Удаление админа проекта

```
1. SuperAdmin/ProjectSuperAdmin удаляет админа
   ↓
2. Проверка прав:
   ├─ superAdmin → может удалить любого
   ├─ projectSuperAdmin → может удалить только в своем проекте
   └─ projectSuperAdmin → НЕ может удалить себя
   ↓
3. Удаление записи из project_admins
   ↓
4. Пользователь теряет доступ к проекту
```

---

## 🗂️ Структура файлов

```
packages/auth-sdk/src/server/
  ├── role-service.ts (обновить)
  ├── project-admin-service.ts (новый)
  └── middleware.ts (обновить)

lib/project-admins/
  ├── types.ts (новый)
  ├── service.ts (новый)
  └── client-service.ts (новый)

app/api/projects/[projectId]/admins/
  └── route.ts (новый)

app/projects/[projectId]/admins/
  ├── page.tsx (новый)
  └── components/
      └── AdminForm.tsx (новый)

supabase/migrations/
  └── 20250130_xxxxx_create_project_admins.sql (новый)
```

---

## 🔐 Ключевые проверки прав

### Кто может управлять админами проекта?

```typescript
function canManageProjectAdmins(projectId: string, userId: string): boolean {
  // 1. SuperAdmin может управлять всеми проектами
  if (isSuperAdmin(userId)) return true;

  // 2. ProjectSuperAdmin может управлять только своим проектом
  if (isProjectSuperAdmin(projectId, userId)) return true;

  return false;
}
```

### Кто может видеть проект?

```typescript
function canViewProject(projectId: string, userId: string): boolean {
  // 1. SuperAdmin видит все проекты автоматически (НЕ в project_admins!)
  if (isSuperAdmin(userId)) return true;

  // 2. ProjectSuperAdmin/ProjectAdmin видят только свои проекты (из project_admins)
  return isProjectAdmin(projectId, userId);
}
```

### ⚠️ ВАЖНО: SuperAdmin НЕ в project_admins

**Концепция:**

- SuperAdmin имеет доступ ко всем проектам **автоматически** через роль `superAdmin`
- SuperAdmin **НЕ добавляется** в таблицу `project_admins`
- В списках админов проекта superAdmin **НЕ показывается**
- Проверка доступа: сначала проверяем `isSuperAdmin()`, потом `project_admins`

### Кто может управлять структурой данных?

```typescript
function canManageEntityDefinitions(
  projectId: string,
  userId: string
): boolean {
  // 1. SuperAdmin может управлять всеми проектами
  if (isSuperAdmin(userId)) return true;

  // 2. ProjectSuperAdmin может управлять только своим проектом
  return isProjectSuperAdmin(projectId, userId);

  // ProjectAdmin НЕ может
}
```

---

## 📊 Таблица прав доступа

| Роль              | Проекты     | Управление админами | Управление данными               | Управление структурой |
| ----------------- | ----------- | ------------------- | -------------------------------- | --------------------- |
| superAdmin        | Все         | Все проекты         | Все проекты                      | Все проекты           |
| projectSuperAdmin | Только свой | Только свой проект  | Только свой проект               | Только свой проект    |
| projectAdmin      | Только свой | ❌                  | Только свой проект (ограниченно) | ❌                    |

---

## ✅ Чеклист реализации

### База данных

- [ ] Создать `project_admin_roles`
- [ ] Создать `project_admins`
- [ ] Создать RLS политики
- [ ] Создать функции проверки ролей

### Backend

- [ ] Обновить `role-service.ts`
- [ ] Создать `project-admin-service.ts`
- [ ] Обновить middleware

### API

- [ ] Создать `/api/projects/[projectId]/admins` endpoints
- [ ] Добавить проверку прав в каждый endpoint

### Frontend

- [ ] Создать страницу управления админами проекта
- [ ] Обновить навигацию (показывать только доступные проекты)
- [ ] Скрыть недоступные функции для projectAdmin

---

**Дата создания:** 2025-01-30  
**Статус:** План реализации
