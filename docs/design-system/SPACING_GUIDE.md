# Руководство по Spacing (Отступам) в проекте

**Версия:** 1.0  
**Обновлено:** Январь 2025  
**Основа:** Tailwind CSS spacing scale

---

## 📐 Базовые принципы

### Единая система отступов

Проект использует **Tailwind CSS spacing scale** с базовой единицей **4px**:

| Класс | Значение | Пиксели | Применение |
|-------|----------|---------|------------|
| `gap-1` | 0.25rem | **4px** | Микро-отступы (иконки, мелкие элементы) |
| `gap-2` | 0.5rem | **8px** | Малые отступы (элементы в группах) |
| `gap-4` | 1rem | **16px** | Средние отступы (связанные элементы, padding) |
| `gap-6` | 1.5rem | **24px** | Большие отступы (между секциями) |
| `gap-8` | 2rem | **32px** | Очень большие отступы (крупные блоки) |

**Важно:** Все spacing делается через Tailwind классы, константы не используются.

---

## 🎯 Правила применения

### 1. Navbar → Breadcrumbs

**Проблема:** Breadcrumbs прилегает прямо к Navbar без отступа.

**Решение:** Добавить `pt-4` (16px padding-top) к компоненту Breadcrumbs.

**Обоснование:**
- Соответствует вертикальному padding Navbar (`py-4` = 16px)
- Обеспечивает визуальное разделение
- Соответствует общей системе spacing

**Пример:**
```tsx
// В компоненте Breadcrumbs
<Breadcrumb className="pt-4">
  {/* ... */}
</Breadcrumb>
```

---

### 2. Breadcrumbs → Контент страницы

**Стандарт:** `space-y-6` (24px) между Breadcrumbs и основным контентом.

**Применение:**
```tsx
<div className="space-y-6">
  <Breadcrumbs {...props} />
  {/* Основной контент */}
</div>
```

**Обоснование:**
- Уже используется на большинстве страниц
- Обеспечивает достаточное визуальное разделение
- Соответствует spacing между секциями

---

### 3. Sidebar внутренние элементы

**Стандарт:** `gap-2` (8px) для элементов внутри Sidebar.

**Применение:**
- SidebarHeader, SidebarFooter: `p-2` (базовый) + дополнительные классы
- SidebarGroup: `gap-2` для внутренних элементов
- SidebarMenu: `gap-1` для items

**Текущее использование:**
```tsx
// SidebarHeader
<SidebarHeader className="py-4"> {/* p-2 базовый + py-4 дополнительный */}
  {/* ... */}
</SidebarHeader>

// SidebarContent
<SidebarContent className="gap-2">
  {/* ... */}
</SidebarContent>
```

**Важно:** Sidebar является источником истины для spacing паттернов.

---

### 4. Layout wrapper

**Стандарт:** `px-4` (16px) горизонтальный padding для основного контента.

**Текущее использование:**
```tsx
// app/layout.tsx
<main className="w-full">
  <Navbar />
  <div className="px-4">{children}</div>
</main>
```

**Обоснование:**
- Обеспечивает отступ от краев экрана
- Стандартный padding для контентных областей
- Соответствует средним отступам (16px)

---

### 5. Секции на страницах

**Стандарт:** `space-y-6` (24px) между основными секциями страницы.

**Применение:**
```tsx
<div className="space-y-6">
  <Breadcrumbs {...props} />
  <Section1 />
  <Section2 />
  <Section3 />
</div>
```

**Когда использовать:**
- Между основными блоками контента
- Между формой и списком
- Между заголовком и контентом

**Альтернативы:**
- `space-y-4` (16px) - для тесно связанных секций
- `space-y-8` (32px) - для крупных независимых блоков

---

### 6. Элементы внутри компонентов

**Стандарт:** `gap-2` (8px) для связанных элементов внутри компонента.

**Применение:**
```tsx
// Группа кнопок
<div className="flex items-center gap-2">
  <Button>Save</Button>
  <Button variant="outline">Cancel</Button>
</div>

// Форма с полями
<div className="flex flex-col gap-2">
  <Input />
  <Input />
</div>
```

**Когда использовать:**
- Кнопки в группе
- Поля формы
- Элементы навигации
- Иконки с текстом

---

### 7. Микро-отступы

**Стандарт:** `gap-1` (4px) для очень близких элементов.

**Применение:**
```tsx
// Иконка рядом с текстом
<div className="flex items-center gap-1">
  <Icon />
  <span>Text</span>
</div>

// Мелкие элементы в списке
<div className="flex gap-1">
  <Badge>Tag1</Badge>
  <Badge>Tag2</Badge>
</div>
```

---

## 📋 Чек-лист для новых страниц

При создании новой страницы проверьте:

- [ ] Breadcrumbs имеет `pt-4` (отступ от Navbar)
- [ ] Контейнер страницы использует `space-y-6` для основных секций
- [ ] Группы элементов используют `gap-2`
- [ ] Layout wrapper имеет `px-4` (проверьте в layout.tsx)
- [ ] Spacing соответствует существующим страницам

---

## 🎨 Примеры использования

### Пример 1: Типичная страница со списком

```tsx
export default function EntityListPage() {
  return (
    <div className="space-y-6">
      <Breadcrumbs 
        projectId={projectId}
        projectName={project.name}
        className="pt-4" // Отступ от Navbar
      />
      
      {description && (
        <p className="text-muted-foreground">{description}</p>
      )}

      <EntityList {...props} />
    </div>
  );
}
```

### Пример 2: Страница с формой

```tsx
export default function EntityFormPage() {
  return (
    <div className="space-y-6">
      <Breadcrumbs {...props} className="pt-4" />
      
      <div className="rounded-lg border bg-card p-6">
        <form className="flex flex-col gap-4">
          <Input label="Name" />
          <Input label="Description" />
          
          <div className="flex items-center gap-2">
            <Button>Save</Button>
            <Button variant="outline">Cancel</Button>
          </div>
        </form>
      </div>
    </div>
  );
}
```

### Пример 3: Компонент с группой элементов

```tsx
function ActionBar() {
  return (
    <div className="flex items-center gap-2">
      <Button>Primary</Button>
      <Button variant="outline">Secondary</Button>
      <DropdownMenu>
        <DropdownMenuTrigger>
          <Button variant="ghost">
            <MoreIcon className="h-4 w-4" />
          </Button>
        </DropdownMenuTrigger>
      </DropdownMenu>
    </div>
  );
}
```

---

## 🔍 Справочная таблица

| Контекст | Класс | Значение | Когда использовать |
|----------|-------|----------|-------------------|
| **Navbar → Breadcrumbs** | `pt-4` | 16px | Всегда для Breadcrumbs |
| **Breadcrumbs → Контент** | `space-y-6` | 24px | Между Breadcrumbs и основным контентом |
| **Секции страницы** | `space-y-6` | 24px | Между основными блоками |
| **Связанные секции** | `space-y-4` | 16px | Для тесно связанных блоков |
| **Крупные блоки** | `space-y-8` | 32px | Для независимых крупных блоков |
| **Элементы в группе** | `gap-2` | 8px | Кнопки, поля формы, навигация |
| **Микро-элементы** | `gap-1` | 4px | Иконки с текстом, мелкие элементы |
| **Layout padding** | `px-4` | 16px | Горизонтальный padding контента |
| **Sidebar элементы** | `gap-2` | 8px | Внутренние элементы Sidebar |

---

## ⚠️ Важные правила

### ❌ НЕ делайте так:

```tsx
// НЕ используйте произвольные значения
<div className="gap-[13px]"> {/* ❌ */}

// НЕ используйте margin вместо gap/padding без необходимости
<div className="mt-5"> {/* ❌ Используйте space-y-6 */}

// НЕ смешивайте разные системы
<div className="gap-2 space-y-4"> {/* ❌ Выберите один подход */}
```

### ✅ Делайте так:

```tsx
// Используйте стандартные Tailwind классы
<div className="gap-2"> {/* ✅ */}

// Используйте space-y для вертикальных отступов
<div className="space-y-6"> {/* ✅ */}

// Используйте padding для отступов от краев
<div className="px-4 py-6"> {/* ✅ */}
```

---

## 🔄 Миграция существующих страниц

При обновлении существующих страниц:

1. **Проверьте Breadcrumbs:**
   - Добавьте `pt-4` если отсутствует отступ от Navbar

2. **Проверьте spacing между секциями:**
   - Замените нестандартные значения на `space-y-6`
   - Используйте `space-y-4` для тесно связанных секций

3. **Проверьте группы элементов:**
   - Замените произвольные `gap-*` на стандартные (`gap-1`, `gap-2`, `gap-4`)

4. **Проверьте padding:**
   - Убедитесь, что используется `px-4` в layout wrapper
   - Проверьте padding внутри карточек (`p-6` стандарт)

---

## 📚 Дополнительные ресурсы

- [Tailwind CSS Spacing Scale](https://tailwindcss.com/docs/customizing-spacing)
- [shadcn/ui Components](https://ui.shadcn.com/) - примеры spacing в компонентах
- Sidebar компоненты (`components/ui/sidebar.tsx`) - источник истины для spacing паттернов

---

## 💡 Частые вопросы

**Q: Какой spacing использовать между формой и списком?**  
A: `space-y-6` (24px) - стандартный отступ между секциями.

**Q: Можно ли использовать `gap-3` (12px)?**  
A: Лучше использовать `gap-2` (8px) или `gap-4` (16px) для единообразия.

**Q: Какой отступ между кнопками в группе?**  
A: `gap-2` (8px) - стандарт для элементов в группе.

**Q: Нужен ли отступ между Navbar и Breadcrumbs?**  
A: Да, всегда используйте `pt-4` (16px) на Breadcrumbs.

**Q: Какой spacing в Sidebar?**  
A: `gap-2` (8px) для внутренних элементов - это источник истины.

---

**Последнее обновление:** Январь 2025  
**Поддерживается:** Команда разработки

