# Пошаговая реализация системы ролей

## 📋 Этапы реализации

### Этап 1: Подготовка базы данных ✅

1. **Выполнить базовую миграцию** (`SUPABASE_MIGRATION.sql`)

   - Создает таблицу `profiles`
   - Настраивает RLS

2. **Выполнить миграцию ролей** (`SUPABASE_ROLES_MIGRATION.sql`)

   - Создает таблицы `admin_roles` и `admins`
   - Настраивает RLS политики
   - Создает функции для работы с ролями

3. **Создать суперадмина**
   - Запустить скрипт `scripts/create-super-admin.ts`
   - Или создать вручную через Supabase Dashboard

### Этап 2: Утилиты для работы с ролями

Создать `lib/auth/roles.ts`:

- `getUserRole(userId)` - получить роль пользователя
- `isAdmin(userId)` - проверка, является ли админом
- `isSuperAdmin(userId)` - проверка, является ли суперадмином
- `canManageAdmins(userId)` - проверка прав на управление админами

### Этап 3: Обновление типов

Обновить `lib/supabase/types.ts`:

- Добавить типы для `admin_roles` и `admins`
- Добавить типы для ролей

### Этап 4: Обновление Middleware

Обновить `middleware.ts`:

- После проверки авторизации проверять роль
- Если `user` → редирект на `/welcome`
- Если `admin`/`superAdmin` → доступ к дашборду

### Этап 5: Создание публичной страницы

Создать `app/welcome/page.tsx`:

- Приветствие для обычных пользователей
- Кнопка logout
- Информация о том, что доступ ограничен

### Этап 6: API для управления админами

Создать API routes:

- `app/api/admin/admins/route.ts` - управление админами
- `app/api/admin/users/route.ts` - создание пользователей

### Этап 7: UI для управления админами

Создать `app/admin/admins/page.tsx`:

- Список админов
- Форма добавления админа
- Возможность удаления/изменения роли

---

## 🚀 Быстрый старт

1. Выполните SQL миграции
2. Запустите скрипт создания суперадмина
3. Войдите как суперадмин
4. Начните добавлять других админов

---

## 📝 Следующие шаги

После выполнения миграций и создания суперадмина, можно приступать к реализации остальных этапов.
