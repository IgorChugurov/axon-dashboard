# Инструкция по настройке Supabase и Google OAuth

## 📋 Шаг 1: Настройка Supabase

### 1.1 Создание проекта

1. Перейдите на [https://app.supabase.com](https://app.supabase.com)
2. Нажмите **"New Project"**
3. Заполните:
   - **Name**: название проекта (например, "chadcn-app")
   - **Database Password**: создайте надежный пароль (сохраните его!)
   - **Region**: выберите ближайший регион
4. Нажмите **"Create new project"**
5. Дождитесь завершения создания проекта (2-3 минуты)

### 1.2 Получение API ключей

1. В Supabase Dashboard перейдите в **Settings** → **API**
2. Скопируйте следующие значения (они понадобятся для `.env.local`):
   - **Project URL** → `NEXT_PUBLIC_SUPABASE_URL`
   - **anon public** ключ → `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - **service_role** ключ → `SUPABASE_SERVICE_ROLE_KEY` (⚠️ опционально, только для сервера! См. `SUPABASE_SERVICE_ROLE_KEY.md`)

**Примечание**: Для базовой авторизации Service Role Key **НЕ обязателен**. Он нужен только для админских операций.

### 1.3 Включение Email/Password провайдера

1. Перейдите в **Authentication** → **Providers**
2. Найдите **Email** в списке провайдеров
3. Нажмите на **Email** и убедитесь, что он **Enabled**
4. Настройте (опционально):
   - **Confirm email**: включите, если нужна подтверждение email
   - **Secure email change**: включите для безопасности

---

## 📋 Шаг 2: Настройка Google OAuth

### 2.1 Создание OAuth клиента в Google Cloud Console

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий:
   - Нажмите на выпадающий список проектов вверху
   - Нажмите **"New Project"**
   - Введите название (например, "chadcn-oauth")
   - Нажмите **"Create"**

### 2.2 Включение Google+ API

1. В Google Cloud Console перейдите в **APIs & Services** → **Library**
2. Найдите **"Google+ API"** или **"Google Identity Services API"**
3. Нажмите **"Enable"**

### 2.3 Создание OAuth 2.0 Credentials

1. Перейдите в **APIs & Services** → **Credentials**
2. Нажмите **"+ CREATE CREDENTIALS"** → **"OAuth client ID"**
3. Если появится предупреждение о конфигурации экрана согласия:

   - Нажмите **"Configure Consent Screen"**
   - Выберите **"External"** (для тестирования) или **"Internal"** (для G Suite)
   - Заполните обязательные поля:
     - **App name**: название вашего приложения
     - **User support email**: ваш email
     - **Developer contact information**: ваш email
   - Нажмите **"Save and Continue"**
   - Пропустите шаги Scopes и Test users (можно добавить позже)
   - Нажмите **"Back to Dashboard"**

4. Вернитесь в **Credentials** и снова нажмите **"+ CREATE CREDENTIALS"** → **"OAuth client ID"**
5. Выберите **Application type**: **"Web application"**
6. Введите **Name**: например, "ChadCN App OAuth" или "Our site"
7. В разделе **Authorised JavaScript origins** (опционально, но рекомендуется):

   Нажмите **"+ Add URI"** и добавьте:

   - Для продакшена: `https://yourdomain.com`
   - Для локальной разработки: `http://localhost:3000` (или ваш порт, например `3001`)

   ⚠️ **Примечание**: Это поле необязательно для базовой настройки, но рекомендуется для безопасности.

8. В разделе **Authorised redirect URIs**:

   Нажмите **"+ Add URI"** и добавьте:

   ```
   https://<your-project-ref>.supabase.co/auth/v1/callback
   ```

   Где `<your-project-ref>` - это ID вашего проекта Supabase (можно найти в URL: `https://app.supabase.com/project/<project-ref>`)

   **Пример:**

   ```
   https://vlwxotouwvzvrtguqlwv.supabase.co/auth/v1/callback
   ```

   ⚠️ **Важно**:

   - ✅ **ОБЯЗАТЕЛЬНО**: Добавьте Supabase callback URL (это главное!)
   - ⚠️ **Опционально**: Если вы хотите добавить localhost callback, используйте ваш реальный порт:
     - Если используете порт 3000: `http://localhost:3000/auth/callback`
     - Если используете порт 3001: `http://localhost:3001/auth/callback`
     - И т.д.
   - 💡 **Рекомендация**: Обычно достаточно только Supabase callback URL, так как Supabase обрабатывает редирект автоматически

9. Нажмите **"Create"**
10. **Важно! Скопируйте и сохраните сразу после создания:**
    - **Client ID** (похож на: `123456789-abcdefghijklmnop.apps.googleusercontent.com`)
      - Будет показан в модальном окне после создания
      - Также можно найти в списке OAuth 2.0 Client IDs в Google Cloud Console
    - **Client secret** (похож на: `GOCSPX-abcdefghijklmnopqrstuvwxyz`)
      - ⚠️ **Внимание**: Client Secret показывается только один раз при создании!
      - Если вы его не скопировали, нужно будет создать новый OAuth client

### 2.4 Настройка Google в Supabase

⚠️ **ВАЖНО**: Без включения провайдера в Supabase вы получите ошибку `"Unsupported provider: provider is not enabled"` при попытке входа!

1. Вернитесь в Supabase Dashboard
2. Перейдите в **Authentication** → **Providers**
3. Найдите **Google** в списке провайдеров
4. **ОБЯЗАТЕЛЬНО**: Нажмите на **Google** и включите переключатель **"Enable Sign in with Google"** (он станет зеленым)
   - ⚠️ **Это критически важно!** Без этого шага Google OAuth не будет работать
5. В разделе **Client IDs**:
   - Вставьте **Client ID** из Google Cloud Console (тот, что вы скопировали на шаге 2.3.10)
   - ⚠️ **Важно**: Client ID обязателен! Без него Google sign-in не будет работать
6. В разделе **Client Secret (for OAuth)**:
   - Вставьте **Client Secret** из Google Cloud Console
   - Используйте иконку глаза, чтобы показать/скрыть секрет при вводе
7. **Проверьте Callback URL**:
   - В разделе **Callback URL (for OAuth)** вы увидите URL вида: `https://<your-project-ref>.supabase.co/auth/v1/callback`
   - Этот URL должен совпадать с тем, что вы добавили в Google Cloud Console на шаге 2.3.8
   - Если не совпадает - скопируйте этот URL и добавьте его в Google Cloud Console
8. Нажмите **"Save"** (зеленая кнопка внизу справа)

### 2.5 Проверка настройки

1. В Supabase Dashboard в разделе **Authentication** → **Providers** → **Google**
2. Убедитесь, что видите зеленую галочку и статус **"Enabled"**
3. Проверьте, что **Callback URL** совпадает с тем, что вы добавили в Google Cloud Console
4. Убедитесь, что **Client ID** заполнен (не должно быть красной рамки с ошибкой)

### 2.6 Решение типичных проблем

**Проблема: "Unsupported provider: provider is not enabled" или "validation_failed"**

- **Причина**: Google провайдер не включен в настройках Supabase
- **Решение**:
  1. Перейдите в Supabase Dashboard → **Authentication** → **Providers**
  2. Найдите **Google** в списке провайдеров
  3. Убедитесь, что переключатель **"Enable Sign in with Google"** включен (зеленый)
  4. Если он выключен - включите его
  5. Убедитесь, что заполнены **Client ID** и **Client Secret**
  6. Нажмите **"Save"**
  7. Подождите несколько секунд и попробуйте войти снова

**Проблема: Видите URL вида `https://<project-ref>.supabase.co/auth/v1/authorize?provider=google&redirect_to=http://localhost:3001/auth/callback`**

- **Это нормально!** Этот URL генерируется Supabase для OAuth flow
- Если получаете ошибку, проверьте:
  1. ✅ Google провайдер включен в Supabase (см. выше)
  2. ✅ В Google Cloud Console добавлен redirect URI: `https://<your-project-ref>.supabase.co/auth/v1/callback`
  3. ⚠️ Если вы добавили localhost callback в Google Cloud Console, убедитесь, что порт совпадает:
     - Если в URL видите `localhost:3001`, добавьте `http://localhost:3001/auth/callback` в Google Cloud Console
     - Или просто используйте только Supabase callback URL (рекомендуется)

**Проблема: "At least one Client ID is required when Google sign-in is enabled"**

- **Решение**: Убедитесь, что вы вставили Client ID в поле **Client IDs** в Supabase
- Client ID должен быть в формате: `123456789-abcdefghijklmnop.apps.googleusercontent.com`

**Проблема: "redirect_uri_mismatch" при попытке входа**

- **Причина**: Redirect URI в Google Cloud Console не совпадает с тем, что использует Supabase
- **Решение**:
  1. Скопируйте Callback URL из Supabase (раздел **Callback URL (for OAuth)**)
     - Должен быть вида: `https://<your-project-ref>.supabase.co/auth/v1/callback`
  2. Перейдите в Google Cloud Console → **Credentials** → ваш OAuth client
  3. Убедитесь, что этот URL добавлен в **Authorised redirect URIs**
  4. Если вы добавили localhost callback, убедитесь, что порт совпадает с тем, что использует ваше приложение:
     - Проверьте, на каком порту запущено приложение (например, `localhost:3001`)
     - Добавьте соответствующий redirect URI: `http://localhost:3001/auth/callback`
  5. Сохраните изменения в Google Cloud Console
  6. Подождите 5-10 минут (изменения могут применяться с задержкой)

**Проблема: Client Secret не был скопирован**

- **Решение**:
  1. В Google Cloud Console перейдите в **Credentials**
  2. Найдите ваш OAuth client и нажмите на него
  3. Если Client Secret не виден, создайте новый OAuth client
  4. На этот раз обязательно скопируйте Client Secret сразу после создания

**Проблема: OAuth consent screen не настроен**

- **Решение**:
  1. В Google Cloud Console перейдите в **APIs & Services** → **OAuth consent screen**
  2. Заполните обязательные поля (App name, User support email, Developer contact information)
  3. Сохраните и вернитесь к созданию OAuth client

---

## 📋 Шаг 3: Настройка GitHub OAuth (для будущего)

Когда будете готовы добавить GitHub:

1. Перейдите в [GitHub Developer Settings](https://github.com/settings/developers)
2. Нажмите **"New OAuth App"**
3. Заполните:
   - **Application name**: название приложения
   - **Homepage URL**: `http://localhost:3000` (или ваш порт, например `3001`)
   - **Authorization callback URL**:
     ```
     https://<your-project-ref>.supabase.co/auth/v1/callback
     ```
4. Нажмите **"Register application"**
5. Скопируйте **Client ID** и создайте **Client Secret**
6. В Supabase Dashboard добавьте эти данные в **Authentication** → **Providers** → **GitHub**

---

## 📋 Шаг 4: Настройка переменных окружения

Создайте файл `.env.local` в корне проекта:

```env
# Supabase (обязательные)
NEXT_PUBLIC_SUPABASE_URL=https://<your-project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<your-anon-key>

# Service Role Key (опционально, только для админских операций)
# SUPABASE_SERVICE_ROLE_KEY=<your-service-role-key>

# Для разработки (опционально)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
```

⚠️ **Важно**:

- Не коммитьте `.env.local` в Git (он уже должен быть в `.gitignore`)
- `SUPABASE_SERVICE_ROLE_KEY` опционален для базовой авторизации
- Если используете Service Role Key - только на сервере, никогда не экспортируйте на клиент!
- Подробнее см. `SUPABASE_SERVICE_ROLE_KEY.md`

---

## ✅ Чеклист готовности

Перед началом разработки убедитесь:

- [ ] Проект Supabase создан
- [ ] Получены все API ключи (URL, anon key, service role key)
- [ ] Email провайдер включен в Supabase
- [ ] Google OAuth клиент создан в Google Cloud Console
- [ ] Redirect URI добавлен в Google Cloud Console
- [ ] Google провайдер настроен в Supabase Dashboard
- [ ] Переменные окружения добавлены в `.env.local`
- [ ] Проект готов к разработке! 🚀

---

## 🔗 Полезные ссылки

- [Supabase Dashboard](https://app.supabase.com)
- [Google Cloud Console](https://console.cloud.google.com/)
- [Supabase Auth Docs](https://supabase.com/docs/guides/auth)
- [Google OAuth Setup](https://supabase.com/docs/guides/auth/social-login/auth-google)
