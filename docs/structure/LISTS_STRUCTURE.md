# Структура использования списков в проекте

**Дата создания:** 2025-01-20  
**Последнее обновление:** 2025-01-20  
**Версия:** 1.0

---

## 📋 Содержание

1. [Обзор](#обзор)
2. [Универсальные компоненты списков](#универсальные-компоненты-списков)
3. [Страницы со списками](#страницы-со-списками)
4. [Сервисы для загрузки данных](#сервисы-для-загрузки-данных)
5. [Проблемы и недостатки](#проблемы-и-недостатки)
6. [План рефакторинга](#план-рефакторинга)

---

## Обзор

В проекте используются списки для отображения различных сущностей. Есть универсальный компонент `UniversalEntityList`, который должен работать по аналогии с универсальными формами - использовать конфигурацию из `EntityUIConfig` и сервисы для загрузки данных.

### Статистика

- **Универсальных компонентов:** 1 (`UniversalEntityList`)
- **Устаревших компонентов:** 1 (`EntityList` - не используется)
- **Страниц со списками:** 4 страницы (кроме списка проектов)
- **Сервисов для загрузки:** Различные (прямые вызовы, API routes, сервисы)

---

## Универсальные компоненты списков

### 1. UniversalEntityList

**Расположение:** `components/UniversalEntityList.tsx` (489 строк)

**Описание:** Универсальный компонент для отображения списка любой сущности. Использует `EntityUIConfig` для автоматической настройки UI.

**Текущие возможности:**

- ✅ Заголовки и текст из `uiConfig.list`
- ✅ Кнопки создания
- ✅ Поиск (базовая реализация)
- ✅ Пустое состояние
- ✅ Таблица с колонками из конфигурации
- ⚠️ Пагинация (частично реализована, но не работает с сервисами)
- ❌ Фильтры (структура готова, но не реализована)
- ❌ Загрузка данных через сервисы (использует `initialInstances`)

**Проблемы:**

1. **Не использует сервисы для загрузки данных:**

   - Принимает `initialInstances` как проп
   - Не перезагружает данные при изменении страницы/поиска
   - Пагинация работает только через URL, но данные не обновляются

2. **Поиск не работает:**

   - Обновляет URL, но не загружает новые данные
   - Нет интеграции с сервисами поиска

3. **Пагинация не работает:**

   - Обновляет URL и локальное состояние `page`
   - Но не загружает данные для новой страницы
   - Проверка `instances.length < pageSize` неверна (всегда показывает Next)

4. **Хардкод роутинга:**

   - Логика навигации захардкожена в компоненте
   - Разные условия для разных типов сущностей (`entity_definition`, `field`, `environments`)

5. **Нет интеграции с сервисами:**
   - Не использует `entity-service` или `instance-service`
   - Не использует API routes для загрузки данных

**Интерфейс:**

```typescript
interface UniversalEntityListProps {
  entityDefinition: EntityDefinition;
  uiConfig: EntityUIConfig;
  initialInstances: any[]; // ❌ Проблема: данные передаются как проп
  initialPage?: number;
  initialSearch?: string;
  projectId: string;
  fields?: any[];
}
```

---

### 2. EntityList (устаревший)

**Расположение:** `components/EntityList.tsx` (178 строк)

**Описание:** Старый компонент для списков с поддержкой пагинации и поиска. Не используется в проекте.

**Статус:** ❌ Устаревший, не используется

---

## Страницы со списками

### 1. Список Entity Definitions

**Путь:** `/projects/[projectId]`  
**Файл:** `app/projects/[projectId]/page.tsx`

**Использует:** `UniversalEntityList`

**Как работает:**

1. Загружает `entityDefinitions` через `getEntityDefinitions(projectId)`
2. Загружает UI конфиг через `loadUIConfigFromFile("entity-definitions")`
3. Создает фиктивную `EntityDefinition` для списка
4. Передает все данные в `UniversalEntityList` как `initialInstances`

**Проблемы:**

- ❌ Нет пагинации (загружает все entityDefinitions сразу)
- ❌ Нет поиска (поиск в компоненте не работает)
- ❌ Не использует сервисы для загрузки данных

**Код:**

```typescript
const entityDefinitions = await getEntityDefinitions(projectId);
const uiConfig = loadUIConfigFromFile("entity-definitions");

<UniversalEntityList
  entityDefinition={entityDefinitionForList}
  fields={[]}
  uiConfig={uiConfig}
  initialInstances={entityDefinitions} // ❌ Все данные сразу
  initialPage={1}
  initialSearch=""
  projectId={projectId}
/>;
```

---

### 2. Список Entity Instances

**Путь:** `/projects/[projectId]/entity-instances/[entityDefinitionId]`  
**Файл:** `app/projects/[projectId]/entity-instances/[entityDefinitionId]/page.tsx`

**Использует:** `EntityListClient` (не `UniversalEntityList`!)

**Как работает:**

1. Загружает `entityDefinition` и `fields` через `EntityDefinitionServerWrapper`
2. Загружает `instances` через `getInstances()` с пагинацией
3. Передает данные в `EntityListClient`

**Проблемы:**

- ❌ Использует отдельный компонент `EntityListClient` вместо `UniversalEntityList`
- ❌ `EntityListClient` не использует `EntityUIConfig` для конфигурации
- ❌ Нет поиска (TODO в коде)
- ⚠️ Пагинация частично работает (загружает данные, но не отображает)

**Код:**

```typescript
const instances = await getInstances(entityDefinitionId, projectId, {
  limit: 10,
  offset: (page - 1) * 10,
  includeRelations:
    relationFieldNames.length > 0 ? relationFieldNames : undefined,
  // TODO: Добавить поиск по полям
});

<EntityListClient
  entityDefinition={entityDefinition}
  fields={fields}
  initialInstances={instances}
  initialPage={page}
  initialSearch={search}
/>;
```

---

### 3. Список Fields

**Путь:** `/projects/[projectId]/entity-definition/[entityDefinitionId]/fields`  
**Файл:** `app/projects/[projectId]/entity-definition/[entityDefinitionId]/fields/page.tsx`

**Использует:** `UniversalEntityList`

**Как работает:**

1. Загружает `entityDefinition` и `fields` через `getEntityDefinitionWithFields()`
2. Загружает UI конфиг через `loadUIConfigFromFile("fields")`
3. Создает фиктивную `EntityDefinition` для списка
4. Сортирует поля по `displayIndex`
5. Передает все данные в `UniversalEntityList` как `initialInstances`

**Проблемы:**

- ❌ Нет пагинации (загружает все fields сразу)
- ❌ Нет поиска (поиск в компоненте не работает)
- ❌ Не использует сервисы для загрузки данных

**Код:**

```typescript
const { entityDefinition, fields } = await getEntityDefinitionWithFields(
  entityDefinitionId
);
const uiConfig = loadUIConfigFromFile("fields");
const sortedFields = [...fields].sort(
  (a, b) => a.displayIndex - b.displayIndex
);

<UniversalEntityList
  entityDefinition={entityDefinitionForList}
  fields={[]}
  uiConfig={uiConfig}
  initialInstances={sortedFields} // ❌ Все данные сразу
  initialPage={1}
  initialSearch=""
  projectId={projectId}
/>;
```

---

### 4. Список Environments

**Путь:** `/projects/[projectId]/settings?tab=environments`  
**Файл:** `app/projects/[projectId]/settings/EnvironmentsTab.tsx`

**Использует:** `UniversalEntityList`

**Как работает:**

1. Загружает `environments` через `getEnvironments()` с пагинацией и поиском
2. Загружает UI конфиг через `loadUIConfigFromFile("environments")`
3. Создает фиктивную `EntityDefinition` и `Field[]` для списка
4. Преобразует environments в формат для `UniversalEntityList`
5. Передает данные в `UniversalEntityList`

**Особенности:**

- ✅ Использует сервис `getEnvironments()` с пагинацией
- ✅ Поддерживает поиск на уровне сервиса
- ⚠️ Но `UniversalEntityList` не перезагружает данные при изменении страницы/поиска

**Проблемы:**

- ❌ `UniversalEntityList` не использует пагинацию из сервиса
- ❌ Поиск не работает (обновляет URL, но не загружает новые данные)

**Код:**

```typescript
const { data: environments, pagination } = await getEnvironments(projectId, {
  page: initialPage,
  limit: 20,
  search: initialSearch,
});

const uiConfig = loadUIConfigFromFile("environments");
const instances = environments.map((env) => ({
  id: env.id,
  key: env.key,
  type: env.type,
  value: env.value,
  options: env.options,
  createdAt: env.createdAt,
  updatedAt: env.updatedAt,
}));

<UniversalEntityList
  entityDefinition={entityDefinition}
  fields={fields}
  uiConfig={uiConfig}
  initialInstances={instances} // ❌ Данные передаются как проп
  initialPage={initialPage}
  initialSearch={initialSearch}
  projectId={projectId}
/>;
```

---

## Сервисы для загрузки данных

### 1. Entity Service (`lib/entity-service/base.ts`)

**Описание:** Универсальный сервис для работы с сущностями через Supabase.

**Методы:**

- `getAll(params)` - получение списка с пагинацией, поиском, фильтрами
- `getById(id)` - получение одной записи
- `create(data)` - создание записи
- `update(id, data)` - обновление записи
- `delete(id)` - удаление записи

**Параметры `getAll`:**

```typescript
interface AdvancedServerDataParams {
  page?: number;
  limit?: number;
  search?: string;
  filters?: EntityFilter[];
  sortBy?: string;
  sortOrder?: "asc" | "desc";
}
```

**Возвращает:**

```typescript
interface EntityResponse<T> {
  data: T[];
  pagination: {
    page: number;
    limit: number;
    total: number;
    totalPages: number;
    hasPreviousPage: boolean;
    hasNextPage: boolean;
  };
}
```

**Использование:** Не используется в списках напрямую.

---

### 2. Instance Service (`lib/universal-entity/instance-service.ts`)

**Описание:** Сервис для работы с экземплярами сущностей (entity instances).

**Методы:**

- `getInstances(entityDefinitionId, projectId, options)` - получение списка экземпляров
- `getInstanceById(instanceId, includeRelations)` - получение одного экземпляра

**Параметры `getInstances`:**

```typescript
interface GetInstancesOptions {
  limit?: number;
  offset?: number;
  includeRelations?: string[];
  filters?: Record<string, any>;
  sortBy?: string;
  sortOrder?: "asc" | "desc";
  // TODO: Добавить поиск
}
```

**Использование:** Используется в `EntityListClient` для загрузки экземпляров.

---

### 3. Environment Service (`lib/environments/service.ts`)

**Описание:** Специализированный сервис для работы с environments.

**Методы:**

- `getEnvironments(projectId, params)` - получение списка с пагинацией и поиском

**Параметры:**

```typescript
{
  page?: number;
  limit?: number;
  search?: string;
}
```

**Возвращает:**

```typescript
{
  data: Environment[];
  pagination: {
    page: number;
    limit: number;
    total: number;
    totalPages: number;
    hasPreviousPage: boolean;
    hasNextPage: boolean;
  };
}
```

**Использование:** Используется в `EnvironmentsTab`.

---

### 4. Config Service (`lib/universal-entity/config-service.ts`)

**Описание:** Сервис для работы с EntityDefinitions и Fields.

**Методы:**

- `getEntityDefinitions(projectId)` - получение всех entityDefinitions проекта
- `getEntityDefinitionWithFields(entityDefinitionId)` - получение entityDefinition с полями

**Использование:** Используется в страницах списков для загрузки метаданных.

---

## Проблемы и недостатки

### 1. UniversalEntityList не использует сервисы

**Проблема:** Компонент принимает данные как `initialInstances` и не перезагружает их при изменении страницы/поиска.

**Последствия:**

- Пагинация не работает (обновляет URL, но не загружает новые данные)
- Поиск не работает (обновляет URL, но не загружает новые данные)
- Нет интеграции с сервисами для загрузки данных

**Решение:** Компонент должен принимать сервис или функцию загрузки данных и вызывать её при изменении параметров.

---

### 2. Разные подходы к загрузке данных

**Проблема:** Разные страницы используют разные подходы:

- Entity Definitions: загружает все данные сразу
- Entity Instances: использует `EntityListClient` с частичной пагинацией
- Fields: загружает все данные сразу
- Environments: использует сервис с пагинацией, но компонент не использует её

**Последствия:**

- Нет единообразия
- Сложно поддерживать
- Разные баги в разных местах

**Решение:** Все страницы должны использовать единый подход через `UniversalEntityList` с сервисами.

---

### 3. EntityListClient дублирует функциональность

**Проблема:** `EntityListClient` - отдельный компонент, который дублирует функциональность `UniversalEntityList`, но не использует `EntityUIConfig`.

**Последствия:**

- Дублирование кода
- Разные баги
- Нет единообразия

**Решение:** Мигрировать `EntityListClient` на использование `UniversalEntityList`.

---

### 4. Хардкод роутинга в UniversalEntityList

**Проблема:** Логика навигации захардкожена в компоненте с разными условиями для разных типов сущностей.

**Последствия:**

- Сложно поддерживать
- Нужно добавлять новые условия для новых типов сущностей

**Решение:** Вынести логику роутинга в конфигурацию или использовать универсальные паттерны.

---

### 5. Нет поддержки фильтров

**Проблема:** Структура для фильтров есть в `EntityUIConfig`, но не реализована.

**Последствия:**

- Нельзя фильтровать данные в списках

**Решение:** Реализовать компонент фильтров и интеграцию с сервисами.

---

### 6. Пагинация работает неправильно

**Проблема:** Проверка `instances.length < pageSize` неверна - всегда показывает Next, если данных меньше чем pageSize.

**Последствия:**

- Неправильное отображение кнопки Next
- Нет информации о реальном количестве страниц

**Решение:** Использовать информацию о пагинации из сервиса (`pagination.hasNextPage`).

---

## План рефакторинга

См. [LISTS_REFACTORING_PLAN.md](./LISTS_REFACTORING_PLAN.md)

---

## Сводная таблица страниц со списками

| Страница           | Путь                                                                  | Компонент             | Пагинация   | Поиск       | Сервис                          | Статус               |
| ------------------ | --------------------------------------------------------------------- | --------------------- | ----------- | ----------- | ------------------------------- | -------------------- |
| Entity Definitions | `/projects/[projectId]`                                               | `UniversalEntityList` | ❌          | ❌          | `getEntityDefinitions`          | ⚠️ Нужен рефакторинг |
| Entity Instances   | `/projects/[projectId]/entity-instances/[entityDefinitionId]`         | `EntityListClient`    | ⚠️ Частично | ❌          | `getInstances`                  | ⚠️ Нужен рефакторинг |
| Fields             | `/projects/[projectId]/entity-definition/[entityDefinitionId]/fields` | `UniversalEntityList` | ❌          | ❌          | `getEntityDefinitionWithFields` | ⚠️ Нужен рефакторинг |
| Environments       | `/projects/[projectId]/settings?tab=environments`                     | `UniversalEntityList` | ⚠️ Частично | ⚠️ Частично | `getEnvironments`               | ⚠️ Нужен рефакторинг |

**Легенда:**

- ✅ Работает
- ⚠️ Частично работает
- ❌ Не работает
