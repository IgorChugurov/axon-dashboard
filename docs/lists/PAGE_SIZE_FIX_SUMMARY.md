# Исправление проблемы: изменение размера страницы не перезагружает список

**Дата:** 2025-01-28  
**Статус:** ✅ РЕШЕНО

---

## 🔴 Проблема

При изменении размера страницы список не перезагружался. React Query не видел изменения параметров из-за асинхронности обновления URL в Next.js App Router.

---

## 🔍 Причина

**Проблема:** Асинхронность `startTransition` + чтение из `searchParams`

**Поток (не работал):**
```
1. setParams({ limit: 50 })
   ↓
2. updateURL({ limit: 50 }) → startTransition → router.replace
   ↓
3. searchParams еще не обновился (асинхронно)
   ↓
4. params пересчитывается из СТАРОГО searchParams
   ↓
5. React Query видит СТАРЫЙ queryKey
   ↓
6. Данные НЕ перезагружаются ❌
```

---

## ✅ Решение

**Подход:** Оптимистичное обновление + синхронизация с URL

**Принцип:**
1. Локальный state для оптимистичного обновления (React Query видит изменения сразу)
2. URL синхронизируется асинхронно (источник истины при монтировании)
3. При изменении URL извне → синхронизируем state с URL

**Поток (работает):**
```
1. setParams({ limit: 50 })
   ↓
2. setLocalParams({ limit: 50 }) ← ОПТИМИСТИЧНО (сразу)
   ↓
3. params обновляется (через useMemo)
   ↓
4. React Query видит НОВЫЙ queryKey ← СРАЗУ!
   ↓
5. React Query перезагружает данные ← РАБОТАЕТ! ✅
   ↓
6. updateURL({ limit: 50 }) → startTransition → router.replace ← АСИНХРОННО
   ↓
7. URL обновляется
```

---

## 📝 Изменения в коде

### `use-list-params.ts`

**Было:**
- Чтение параметров напрямую из `searchParams`
- `params` вычислялся из `searchParams` через `useMemo`
- При обновлении → обновлялся только URL → `searchParams` обновлялся асинхронно → `params` не обновлялся сразу

**Стало:**
- Локальный state `localParams` для оптимистичного обновления
- `params` вычисляется из `localParams` через `useMemo`
- При обновлении → обновляется `localParams` сразу → обновляется URL асинхронно
- При изменении URL извне → синхронизируем `localParams` с URL

**Ключевые изменения:**

```typescript
// 1. Локальный state для оптимистичного обновления
const [localParams, setLocalParams] = useState(() => ({
  page: urlPageParsed,
  limit: urlLimitParsed,
  search: urlSearchParsed || undefined,
}));

// 2. Синхронизация с URL при изменении URL извне
useEffect(() => {
  const newParams = {
    page: urlPageParsed,
    limit: urlLimitParsed,
    search: urlSearchParsed || undefined,
  };
  
  if (/* значения изменились */) {
    setLocalParams(newParams);
  }
}, [urlPageParsed, urlLimitParsed, urlSearchParsed]);

// 3. params из localParams (стабилизирован для React Query)
const params = useMemo(
  () => ({
    page: localParams.page,
    limit: localParams.limit,
    search: localParams.search,
  }),
  [localParams.page, localParams.limit, localParams.search]
);

// 4. Оптимистичное обновление
const setParams = (updates) => {
  // Обновляем state сразу (React Query видит изменения)
  setLocalParams(prev => ({ ...prev, ...updates }));
  
  // Синхронизируем с URL (асинхронно)
  updateURL(urlUpdates);
};
```

---

## ✅ Результат

**Работает:**
- ✅ Изменение размера страницы → список перезагружается
- ✅ Переход между страницами → список перезагружается
- ✅ Поиск → список перезагружается
- ✅ Браузерная навигация → синхронизация с URL

**Преимущества:**
- ✅ Мгновенная реакция UI (нет задержки)
- ✅ React Query видит изменения сразу
- ✅ URL остается источником истины при монтировании
- ✅ Нет конфликтов между state и URL

---

## 🔄 Архитектура

**Источники истины:**
- **При монтировании:** URL → инициализирует `localParams`
- **При изменении параметров:** `localParams` → обновляется сразу → URL синхронизируется асинхронно
- **При браузерной навигации:** URL → синхронизирует `localParams`

**Поток данных:**
```
URL (источник при монтировании)
  ↓
localParams (оптимистичное обновление)
  ↓
params (стабилизирован для React Query)
  ↓
React Query (загрузка данных)
```

---

## 📊 Сравнение подходов

| Подход | Реакция UI | React Query видит изменения | URL синхронизация | Сложность |
|--------|------------|------------------------------|-------------------|-----------|
| **Было (только URL)** | ❌ Задержка | ❌ Нет (асинхронность) | ✅ Да | Низкая |
| **Стало (оптимистично)** | ✅ Сразу | ✅ Да | ✅ Да | Средняя |

---

## 🎯 Выводы

**Проблема решена:** Использование оптимистичного обновления позволяет React Query видеть изменения параметров сразу, устраняя проблему асинхронности обновления URL в Next.js App Router.

**Архитектура:** Гибридный подход - локальный state для оптимистичного обновления + синхронизация с URL для поддержания источника истины.

