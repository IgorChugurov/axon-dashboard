# Системный анализ и решение проблемы пагинации

**Дата:** 2025-01-28  
**Проблема:** Изменение размера страницы и переход между страницами не работают

---

## 🔍 Анализ текущей ситуации

### Текущая архитектура

```
URL (searchParams)
  ↓
useListParams (читает из URL)
  ↓
params (useMemo)
  ↓
useListQuery (React Query)
  ↓
Данные
```

### Проблемы

#### 1. Изменение размера страницы не работает

**Что происходит:**

```typescript
// UniversalEntityListDataTable.tsx, строка 366
onPageSizeChange={(newPageSize) => {
  setParams({
    limit: newPageSize,
    page: 1,
  });
}}
```

**Проблема:**

- `setParams` вызывает `updateURL`
- `updateURL` обновляет URL через `router.replace`
- НО: `router.replace` в Next.js App Router может быть асинхронным
- `searchParams` может не обновиться сразу
- `params` пересчитывается из `searchParams`, который еще старый
- React Query не видит изменения `params` (потому что `params` не изменился)

**Корневая причина:** Асинхронность обновления URL в Next.js App Router

---

#### 2. Переход между страницами не работает

**Что происходит:**

```typescript
// UniversalEntityListDataTable.tsx, строка 363
onPageChange={(newPage) => {
  setParams({ page: newPage });
}}
```

**Та же проблема:** Асинхронность обновления URL

---

#### 3. sessionStorage - нужен ли он?

**Текущее использование:**

- Восстановление при первом монтировании (если URL пустой)
- Сохранение при изменении параметров

**Проблемы:**

- Создает дополнительную сложность
- Может конфликтовать с URL
- Не стандартный подход для Next.js

**Стандартный подход Next.js:**

- URL - единственный источник истины
- Если нужна персистентность - использовать cookies или localStorage (но не для пагинации)
- Пагинация должна быть в URL всегда

---

## 📚 Стандартные подходы в индустрии

### Подход 1: URL как единственный источник истины (Next.js стандарт)

**Принцип:**

- Все параметры в URL
- Нет sessionStorage для пагинации
- Если URL пустой → используем defaults
- При изменении → обновляем URL → Next.js перерендеривает → читаем новые параметры

**Преимущества:**

- ✅ Простота
- ✅ Работает с браузерной навигацией
- ✅ Можно делиться ссылками
- ✅ SEO-friendly
- ✅ Стандарт Next.js

**Недостатки:**

- ❌ При возврате на страницу (без URL параметров) сбрасывается на defaults
- ❌ Нет "запоминания" состояния между сессиями

**Когда использовать:**

- Когда не нужна персистентность между сессиями
- Когда URL всегда должен отражать состояние

---

### Подход 2: URL + localStorage для персистентности

**Принцип:**

- URL - источник истины во время работы
- localStorage - для "запоминания" предпочтений пользователя (размер страницы)
- При первом визите → читаем из localStorage → устанавливаем в URL

**Преимущества:**

- ✅ Запоминает предпочтения пользователя
- ✅ URL остается источником истины
- ✅ Работает между сессиями

**Недостатки:**

- ❌ Дополнительная сложность
- ❌ Нужно синхронизировать localStorage и URL

**Когда использовать:**

- Когда нужно запоминать предпочтения пользователя (размер страницы)
- Когда нужна персистентность между сессиями

---

### Подход 3: URL + sessionStorage для восстановления при возврате

**Принцип:**

- URL - источник истины во время работы
- sessionStorage - для восстановления при возврате на страницу (когда URL пустой)
- При возврате → читаем из sessionStorage → восстанавливаем URL

**Преимущества:**

- ✅ Восстанавливает состояние при возврате
- ✅ URL остается источником истины

**Недостатки:**

- ❌ Работает только в рамках одной сессии
- ❌ Дополнительная сложность
- ❌ Может конфликтовать с URL

**Когда использовать:**

- Когда нужно восстанавливать состояние при возврате на страницу
- Когда не нужна персистентность между сессиями

---

## 🎯 Анализ нашего кейса

### Требования:

1. **Пагинация** - должна работать (переход между страницами)
2. **Размер страницы** - должен изменяться
3. **Поиск** - должен работать с debounce
4. **Возврат на страницу** - желательно восстанавливать состояние
5. **Браузерная навигация** - должна работать (Назад/Вперед)

### Проблема с текущим подходом:

**Главная проблема:** Асинхронность обновления URL в Next.js App Router

```typescript
// 1. setParams вызывается
setParams({ page: 2 });

// 2. updateURL обновляет URL
router.replace("?page=2");

// 3. НО searchParams еще не обновился!
// 4. params пересчитывается из старого searchParams
// 5. React Query не видит изменения
```

**Решение:** Нужно использовать оптимистичное обновление или дождаться обновления URL

---

## 💡 Оптимальное решение для нашего кейса

### Вариант 1: URL как единственный источник истины (РЕКОМЕНДУЕТСЯ)

**Принцип:**

- URL - единственный источник истины
- Убрать sessionStorage полностью
- При изменении параметров → обновляем URL → Next.js перерендеривает → читаем новые параметры
- Использовать `startTransition` для оптимистичного обновления

**Преимущества:**

- ✅ Простота
- ✅ Нет конфликтов
- ✅ Стандартный подход Next.js
- ✅ Работает с браузерной навигацией

**Недостатки:**

- ❌ При возврате на страницу (без URL) сбрасывается на defaults
- ❌ Но это нормально - пользователь может добавить в закладки с параметрами

**Реализация:**

```typescript
import { useTransition } from "react";

const [isPending, startTransition] = useTransition();

const setParams = (updates) => {
  startTransition(() => {
    // Обновляем URL
    const params = new URLSearchParams(searchParams.toString());
    if (updates.page) params.set("page", updates.page.toString());
    if (updates.limit) params.set("limit", updates.limit.toString());
    router.replace(`?${params.toString()}`);
  });
};
```

---

### Вариант 2: Оптимистичное обновление + URL синхронизация

**Принцип:**

- Используем локальный state для оптимистичного обновления
- Синхронизируем с URL
- При изменении → обновляем state сразу → обновляем URL → синхронизируем обратно

**Преимущества:**

- ✅ Мгновенная реакция UI
- ✅ URL остается источником истины

**Недостатки:**

- ❌ Дополнительная сложность
- ❌ Нужно синхронизировать state и URL

---

### Вариант 3: Библиотека nuqs (самое простое решение)

**Принцип:**

- Используем готовую библиотеку
- Автоматическая синхронизация с URL
- Решает все проблемы из коробки

**Преимущества:**

- ✅ Решает все проблемы автоматически
- ✅ Стандартное решение для Next.js
- ✅ Type-safe
- ✅ Встроенный debounce

**Недостатки:**

- ❌ Дополнительная зависимость
- ❌ Нужно изучить API

---

## 🔧 Рекомендуемое решение

### Для нашего кейса: **Вариант 1 (URL как единственный источник истины)**

**Почему:**

1. Простота - нет дополнительных зависимостей
2. Стандарт Next.js - соответствует документации
3. Решает проблему асинхронности через `startTransition`
4. Убирает sessionStorage - убирает конфликты

**Что нужно сделать:**

1. **Убрать sessionStorage полностью**

   - Убрать восстановление из sessionStorage
   - Убрать сохранение в sessionStorage
   - Упростить логику

2. **Использовать `startTransition` для обновления URL**

   - Оптимистичное обновление
   - Плавная работа UI

3. **Упростить хук `use-list-params`**
   - Только чтение из URL
   - Только обновление URL
   - Никаких промежуточных состояний

---

## 📋 План реализации

### Этап 1: Упрощение хука (убрать sessionStorage)

**Изменения:**

1. Убрать все вызовы `loadListState` и `saveListState`
2. Убрать логику восстановления из sessionStorage
3. Упростить чтение параметров из URL
4. Добавить `startTransition` для обновления URL

**Результат:**

- Хук станет проще
- Нет конфликтов с sessionStorage
- URL - единственный источник истины

---

### Этап 2: Использование `startTransition`

**Изменения:**

1. Добавить `useTransition` в хук
2. Обернуть обновление URL в `startTransition`
3. Это решит проблему асинхронности

**Результат:**

- Мгновенная реакция UI
- Плавная работа
- Нет задержек

---

### Этап 3: Тестирование

**Что проверить:**

1. Переход между страницами
2. Изменение размера страницы
3. Поиск с debounce
4. Браузерная навигация (Назад/Вперед)
5. Обновление страницы (сохраняются ли параметры в URL)

---

## 🎯 Ожидаемый результат

### После реализации:

1. ✅ **Переход между страницами работает**

   - Обновляется URL
   - React Query перезагружает данные
   - UI обновляется

2. ✅ **Изменение размера страницы работает**

   - Обновляется URL
   - React Query перезагружает данные
   - UI обновляется

3. ✅ **Нет конфликтов**

   - Один источник истины (URL)
   - Нет sessionStorage
   - Простая логика

4. ✅ **Работает с браузерной навигацией**
   - URL изменяется → компонент перерендеривается
   - Параметры читаются из URL
   - Данные загружаются

---

## 📊 Сравнение подходов

| Подход                   | Сложность | Надежность | Стандартность | Рекомендация                        |
| ------------------------ | --------- | ---------- | ------------- | ----------------------------------- |
| **URL только**           | Низкая    | Высокая    | ⭐⭐⭐⭐⭐    | ✅ Рекомендуется                    |
| **URL + sessionStorage** | Средняя   | Средняя    | ⭐⭐⭐        | ⚠️ Текущий (проблемный)             |
| **URL + localStorage**   | Средняя   | Высокая    | ⭐⭐⭐⭐      | ✅ Если нужна персистентность       |
| **nuqs библиотека**      | Низкая    | Высокая    | ⭐⭐⭐⭐      | ✅ Если готовы добавить зависимость |

---

## 🔄 Альтернатива: Если нужна персистентность

Если действительно нужно "запоминать" состояние при возврате на страницу:

**Вариант:** Использовать localStorage для предпочтений пользователя (размер страницы)

**Принцип:**

- `page` - всегда в URL (не персистентно)
- `limit` - в URL, но при первом визите читаем из localStorage (предпочтение пользователя)
- `search` - всегда в URL (не персистентно)

**Но:** Это усложняет логику. Для большинства случаев достаточно URL.

---

## ✅ Итоговая рекомендация

**Использовать подход: URL как единственный источник истины**

**Почему:**

1. ✅ Простота - нет дополнительных зависимостей
2. ✅ Стандарт Next.js - соответствует документации
3. ✅ Решает проблему асинхронности через `startTransition`
4. ✅ Убирает конфликты - нет sessionStorage
5. ✅ Работает с браузерной навигацией

**Что делать:**

1. Убрать sessionStorage полностью
2. Использовать `startTransition` для обновления URL
3. Упростить хук до минимума
4. Протестировать все сценарии

**Результат:**

- Работающая пагинация
- Работающее изменение размера страницы
- Нет конфликтов
- Простая и понятная логика

---

## 📝 Следующие шаги

1. ✅ Анализ завершен
2. ⏳ Реализация упрощенного подхода
3. ⏳ Тестирование
4. ⏳ Документирование
