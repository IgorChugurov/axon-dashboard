# Анализ проблемы: изменение размера страницы не перезагружает список

**Дата:** 2025-01-28  
**Проблема:** При изменении размера страницы список не перезагружается  
**Статус:** ✅ РЕШЕНО (оптимистичное обновление)

---

## 🔍 Анализ потока данных

### Что происходит при изменении размера страницы:

```typescript
// 1. Пользователь выбирает размер страницы 50
onPageSizeChange={(newPageSize) => {
  setParams({
    limit: newPageSize,  // newPageSize = 50
    page: 1,
  });
}}

// 2. setParams вызывает updateURL
updateURL({ limit: 50, page: 1 });

// 3. startTransition обновляет URL
startTransition(() => {
  router.replace('?limit=50', { scroll: false });
});

// 4. ПРОБЛЕМА: startTransition асинхронный!
//    searchParams еще не обновился в этом рендере
//    params пересчитывается из СТАРОГО searchParams
//    React Query видит старый queryKey
```

---

## 🔴 Проблема 1: Асинхронность startTransition

**Проблема:**
- `startTransition` помечает обновление как переход (не срочный)
- `router.replace` выполняется асинхронно
- `searchParams` обновляется в следующем рендере
- Но `params` уже пересчитан из старого `searchParams`
- React Query не видит изменения

**Почему это проблема:**
- React Query использует `queryKey` для определения, нужно ли перезагружать данные
- `queryKey` генерируется из `params`
- Если `params` не изменился, `queryKey` не изменился
- React Query не перезагружает данные

---

## 🔴 Проблема 2: useMemo стабилизирует объект

**Проблема:**
```typescript
const params: LoadParams = useMemo(
  () => ({
    page,
    limit,
    search: search || undefined,
  }),
  [page, limit, search]
);
```

**Если `limit` не изменился (из-за асинхронности):**
- `useMemo` возвращает тот же объект (по ссылке)
- React Query видит тот же `params` объект
- `queryKey` не меняется
- Данные не перезагружаются

---

## 🔴 Проблема 3: Логика удаления limit из URL

**Проблема:**
```typescript
// Если limit равен дефолтному pageSize, не сохраняем в URL
if (updates.limit === pageSize) {
  currentParams.delete("limit");
}
```

**Сценарий:**
1. Пользователь на странице с `limit=20` (дефолтный)
2. URL: `?page=2` (без limit, потому что дефолтный)
3. Пользователь выбирает `limit=50`
4. URL обновляется: `?page=1&limit=50` ✅
5. Но если пользователь вернется к `limit=20`:
   - URL становится: `?page=1` (limit удален)
   - При чтении: `limit = pageSize = 20` (то же значение!)
   - `params` не меняется (limit остался 20)
   - React Query не видит изменения

**Но это не должно быть проблемой для случая, когда выбирается НЕ дефолтное значение.**

---

## 🔍 Диагностика проблемы

### Проверка 1: Обновляется ли URL?

**Нужно проверить:**
- Обновляется ли URL в браузере при изменении размера страницы?
- Если да → проблема в чтении из URL
- Если нет → проблема в обновлении URL

### Проверка 2: Обновляется ли searchParams?

**Нужно проверить:**
- Обновляется ли `searchParams` после `router.replace`?
- Если да → проблема в пересчете `params`
- Если нет → проблема в асинхронности Next.js

### Проверка 3: Обновляется ли params?

**Нужно проверить:**
- Обновляется ли `params` после обновления URL?
- Если да → проблема в React Query
- Если нет → проблема в `useMemo` или чтении из URL

### Проверка 4: Обновляется ли queryKey?

**Нужно проверить:**
- Обновляется ли `queryKey` в React Query?
- Если да → проблема в React Query конфигурации
- Если нет → проблема в генерации queryKey

---

## 💡 Возможные решения

### Решение 1: Использовать оптимистичное обновление

**Принцип:**
- Обновляем `params` сразу (оптимистично)
- Затем обновляем URL
- React Query видит изменение сразу

**Проблема:**
- Нужен промежуточный state
- Конфликт с принципом "URL как источник истины"

---

### Решение 2: Использовать `router.push` вместо `router.replace`

**Принцип:**
- `router.push` может работать по-другому
- Но это не стандартный подход

**Проблема:**
- Не решает проблему асинхронности
- Добавляет запись в историю (не нужно)

---

### Решение 3: Использовать `useSyncExternalStore` для синхронизации с URL

**Принцип:**
- Подписка на изменения URL
- Синхронное обновление состояния

**Проблема:**
- Сложность
- Может не работать с Next.js App Router

---

### Решение 4: Использовать локальный state для оптимистичного обновления + синхронизация с URL

**Принцип:**
- Локальный state для немедленного обновления
- Синхронизация с URL
- URL остается источником истины при монтировании

**Преимущества:**
- ✅ Мгновенная реакция UI
- ✅ React Query видит изменения сразу
- ✅ URL синхронизируется

**Недостатки:**
- ⚠️ Нужен промежуточный state
- ⚠️ Нужна синхронизация state ↔ URL

---

### Решение 5: Использовать библиотеку nuqs (рекомендуется)

**Принцип:**
- Готовая библиотека решает все проблемы
- Автоматическая синхронизация
- Решает проблему асинхронности

**Преимущества:**
- ✅ Решает все проблемы из коробки
- ✅ Стандартное решение
- ✅ Type-safe

**Недостатки:**
- ❌ Дополнительная зависимость

---

## 🎯 Рекомендуемое решение

### Вариант: Оптимистичное обновление + синхронизация с URL

**Принцип:**
1. Локальный state для `params` (оптимистичное обновление)
2. При изменении → обновляем state сразу → обновляем URL
3. При изменении URL извне → синхронизируем state с URL

**Почему это работает:**
- React Query видит изменение `params` сразу
- URL синхронизируется
- Нет задержек

**Реализация:**
```typescript
// Локальный state для оптимистичного обновления
const [localParams, setLocalParams] = useState(() => {
  const page = parseInt(searchParams.get("page") || "1", 10);
  const limit = parseInt(searchParams.get("limit") || pageSize.toString(), 10);
  return { page, limit, search: searchParams.get("search") || undefined };
});

// Синхронизация с URL при изменении URL извне
useEffect(() => {
  const urlPage = parseInt(searchParams.get("page") || "1", 10);
  const urlLimit = parseInt(searchParams.get("limit") || pageSize.toString(), 10);
  const urlSearch = searchParams.get("search") || undefined;
  
  setLocalParams({ page: urlPage, limit: urlLimit, search: urlSearch });
}, [searchParams, pageSize]);

// Обновление параметров
const setParams = (updates) => {
  // Оптимистичное обновление state
  setLocalParams(prev => ({ ...prev, ...updates }));
  
  // Обновление URL
  startTransition(() => {
    // ... обновление URL
  });
};
```

---

## 📋 План диагностики

### Шаг 1: Добавить логирование

**Что логировать:**
- Значения `urlLimit`, `limit`, `params.limit`
- Вызовы `setParams` и `updateURL`
- Изменения `queryKey` в React Query

**Цель:** Понять, где именно теряется обновление

---

### Шаг 2: Проверить обновление URL

**Как проверить:**
- Открыть DevTools → Network
- Изменить размер страницы
- Проверить, обновляется ли URL в адресной строке
- Проверить, есть ли запрос к серверу

**Ожидаемый результат:**
- URL должен обновиться
- Должен быть запрос к серверу

---

### Шаг 3: Проверить обновление searchParams

**Как проверить:**
- Добавить `console.log` в хук
- Логировать `searchParams.get("limit")` до и после обновления

**Ожидаемый результат:**
- `searchParams` должен обновиться после `router.replace`

---

### Шаг 4: Проверить обновление params

**Как проверить:**
- Добавить `console.log` для `params`
- Проверить, меняется ли `params.limit`

**Ожидаемый результат:**
- `params.limit` должен обновиться

---

### Шаг 5: Проверить обновление queryKey

**Как проверить:**
- Добавить `console.log` в `useListQuery`
- Логировать `queryKey` до и после обновления

**Ожидаемый результат:**
- `queryKey` должен измениться (включая новый `limit`)

---

## 🔧 Временное решение для диагностики

Добавить логирование для понимания проблемы:

```typescript
// В use-list-params.ts
console.log('[useListParams]', {
  urlLimit: searchParams.get("limit"),
  limit,
  params,
  pageSize,
});

// В use-list-query.ts
console.log('[useListQuery]', {
  queryKey,
  params,
});
```

Это поможет понять, где именно теряется обновление.

---

## 💡 Выводы

**Проблема:** Асинхронность обновления URL в Next.js App Router + стабилизация через `useMemo` = React Query не видит изменения

**Решения:**
1. ✅ **Оптимистичное обновление (локальный state)** - РЕАЛИЗОВАНО
2. Библиотека nuqs (готовое решение)
3. Другая архитектура (нужно проанализировать глубже)

---

## ✅ РЕАЛИЗОВАННОЕ РЕШЕНИЕ

### Оптимистичное обновление + синхронизация с URL

**Принцип:**
1. Локальный state (`localParams`) для оптимистичного обновления
2. При изменении → обновляем state сразу → React Query видит изменения → обновляем URL
3. При изменении URL извне → синхронизируем state с URL

**Как работает:**

```typescript
// 1. Локальный state для оптимистичного обновления
const [localParams, setLocalParams] = useState(() => ({
  page: urlPageParsed,
  limit: urlLimitParsed,
  search: urlSearchParsed || undefined,
}));

// 2. Синхронизация с URL при изменении URL извне
useEffect(() => {
  const newParams = {
    page: urlPageParsed,
    limit: urlLimitParsed,
    search: urlSearchParsed || undefined,
  };
  
  if (/* значения изменились */) {
    setLocalParams(newParams);
  }
}, [urlPageParsed, urlLimitParsed, urlSearchParsed]);

// 3. Обновление параметров (оптимистично + URL)
const setParams = (updates) => {
  // Оптимистичное обновление state (React Query видит сразу)
  setLocalParams(prev => ({ ...prev, ...updates }));
  
  // Синхронизация с URL (асинхронно)
  updateURL(urlUpdates);
};
```

**Поток при изменении размера страницы:**

```
1. Пользователь выбирает limit=50
   ↓
2. setParams({ limit: 50, page: 1 })
   ↓
3. setLocalParams({ ...prev, limit: 50, page: 1 }) ← ОПТИМИСТИЧНО
   ↓
4. params обновляется (через useMemo)
   ↓
5. React Query видит изменение queryKey ← СРАЗУ!
   ↓
6. React Query перезагружает данные ← РАБОТАЕТ!
   ↓
7. updateURL({ limit: 50, page: 1 }) ← АСИНХРОННО
   ↓
8. URL обновляется через startTransition
```

**Преимущества:**
- ✅ React Query видит изменения сразу (нет задержки)
- ✅ URL синхронизируется (источник истины при монтировании)
- ✅ Работает браузерная навигация (синхронизация state ↔ URL)
- ✅ Нет конфликтов (один источник истины для каждого случая)

**Результат:**
- ✅ Изменение размера страницы работает
- ✅ Переход между страницами работает
- ✅ Поиск работает
- ✅ Браузерная навигация работает

