# Следующие шаги по Universal Entity List

**Дата:** 2025-01-28  
**Для нового чата**

---

## Текущее состояние

### ✅ Что сделано

1. **Интеграция React Query:**

   - Удален State Machine (дублировал функциональность)
   - Используется только React Query для управления состоянием
   - Кеширование данных (30s staleTime, 5min gcTime)
   - Query key: `['list', projectId, serviceType, page, limit, search, filters?, sortBy?, sortOrder?]`

2. **Сохранение состояния:**

   - sessionStorage для page, limit, search
   - Восстановление при возврате на страницу
   - Исправлена проблема с миганием URL и сбросом на первую страницу

3. **Оптимистичные обновления:**

   - Delete с мгновенным обновлением UI
   - Откат при ошибке
   - Инвалидация кеша после успешного удаления

4. **Упрощение кода:**
   - Уменьшение на 27% (с 854 до 620 строк)
   - Удалены неиспользуемые refs и функции
   - Упрощена логика состояний

### 📁 Текущая структура

```
components/
  ├── UniversalEntityListDataTable.tsx  (628 строк)
  ├── UniversalEntityListClient.tsx    (254 строки)
  └── DataTablePagination.tsx
```

---

## План рефакторинга

### Цель: Модульная структура папок

**Новая структура:**

```
components/
  universal-entity-list/
    ├── index.ts                         # Экспорты
    ├── UniversalEntityListDataTable.tsx
    ├── UniversalEntityListClient.tsx
    ├── hooks/
    │   ├── use-list-query.ts           # React Query логика
    │   ├── use-list-params.ts          # Параметры и URL
    │   └── use-list-mutations.ts       # Мутации
    ├── utils/
    │   ├── list-query-key.ts          # Query key генерация
    │   └── list-state-helpers.ts      # Вспомогательные функции
    └── types/
        └── list-types.ts               # Типы
```

### Этапы выполнения

1. **Этап 1:** Создание структуры папок и перемещение файлов
2. **Этап 2:** Вынос хуков (use-list-query, use-list-params, use-list-mutations)
3. **Этап 3:** Вынос утилит (list-query-key, list-state-helpers)
4. **Этап 4:** Вынос типов
5. **Этап 5:** Улучшение компонента (заголовок таблицы, toolbar)

---

## Ключевые файлы

### Основные компоненты

- `components/UniversalEntityListDataTable.tsx` - основной компонент
- `components/UniversalEntityListClient.tsx` - client wrapper
- `components/DataTablePagination.tsx` - пагинация

### Утилиты

- `lib/utils/list-state-storage.ts` - работа с sessionStorage
- `lib/query-client.ts` - настройка QueryClient
- `components/providers/QueryProvider.tsx` - React Query provider

### Конфигурация

- `config/environments.json` - конфигурация для Environments
- Используется статический импорт в `page.tsx`

---

## Важные детали реализации

### React Query

```typescript
const queryKey = [
  "list",
  projectId,
  serviceType,
  loadParams.page,
  loadParams.limit,
  loadParams.search || "",
  ...(loadParams.filters ? [loadParams.filters] : []),
  ...(loadParams.sortBy ? [loadParams.sortBy, loadParams.sortOrder] : []),
] as const;
```

### Сохранение состояния

```typescript
// Сохранение при изменении
useEffect(() => {
  saveListState(projectId, serviceType, {
    page: loadParams.page,
    limit: loadParams.limit,
    search: loadParams.search,
  });
}, [projectId, serviceType, loadParams]);

// Восстановление при монтировании
const storedState = loadListState(projectId, serviceType);
const mergedState = mergeListState(urlState, storedState, defaults);
```

### Оптимистичные обновления

```typescript
const deleteMutation = useMutation({
  mutationFn: async (id: string) => {
    /* ... */
  },
  onMutate: async (id) => {
    // Оптимистично удаляем из кеша
  },
  onError: (error, id, context) => {
    // Откатываем изменения
  },
  onSuccess: () => {
    // Инвалидируем кеш
  },
});
```

---

## Известные проблемы (решены)

1. ✅ **Мигание URL при восстановлении страницы** - исправлено через `needsUrlRestoreRef`
2. ✅ **Сброс на первую страницу** - исправлено через правильный приоритет в `mergeListState`
3. ✅ **Дублирование State Machine и React Query** - решено удалением State Machine

---

## Следующие задачи

### Приоритет 1: Рефакторинг структуры

- [ ] Создать папку `components/universal-entity-list/`
- [ ] Переместить файлы
- [ ] Создать `index.ts` с экспортами
- [ ] Обновить импорты

### Приоритет 2: Вынос логики

- [ ] Создать `hooks/use-list-query.ts`
- [ ] Создать `hooks/use-list-params.ts`
- [ ] Создать `hooks/use-list-mutations.ts`
- [ ] Создать `utils/list-query-key.ts`

### Приоритет 3: Улучшение UI

- [ ] Создать `DataTableHeader.tsx` (заголовок таблицы)
- [ ] Создать `DataTableToolbar.tsx` (toolbar с поиском)
- [ ] Добавить поддержку сортировки в заголовках
- [ ] Добавить поддержку фильтров

### Приоритет 4: Расширенная функциональность

- [ ] Сортировка (server-side)
- [ ] Фильтры (server-side)
- [ ] Выбор строк (row selection)
- [ ] Скрытие колонок (column visibility)

---

## Документация

- `docs/lists/STATUS.md` - полный статус реализации
- `docs/lists/REFACTORING_PLAN.md` - детальный план рефакторинга
- `docs/lists/NEXT_STEPS.md` - этот файл

---

## Для начала работы

1. Прочитать `docs/lists/STATUS.md` для понимания текущего состояния
2. Прочитать `docs/lists/REFACTORING_PLAN.md` для понимания плана
3. Начать с Этапа 1: создание структуры папок

**Важно:** Все изменения должны быть протестированы на странице Environments перед продолжением.
